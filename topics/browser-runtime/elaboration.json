{
  "sections": [
    {
      "anchor": "node-0-a-language-that-cannot-do-anything",
      "title": "Node 0 — A language that cannot do anything",
      "content": "<h3>Where this sits</h3>\n\n<p>This is the opening node of the whole chain. Nothing technical depends on it, but every single later node assumes you have internalised its one claim: JavaScript the language cannot do anything you can observe, and every capability you have ever used from it — printing a value, waiting, fetching, storing — was never part of the language at all. If this node doesn't fully land, every later node will feel like a grab-bag of browser trivia instead of what it actually is: one host's answer to \"what should we bolt onto a language that does nothing by itself?\"</p>\n\n<h3>Every term, from absolute zero</h3>\n\n<p><strong>JavaScript</strong> — the name of the language you write. Confusingly, this is a marketing name, not the technical name of what's actually specified. There is no single canonical rulebook called \"the JavaScript specification.\" The rulebook is called ECMAScript.</p>\n\n<p><strong>ECMAScript</strong> — the actual technical specification of the language. It is a document, formally numbered ECMA-262, published by a standards body. When people say \"the JavaScript spec,\" they mean this document. JavaScript is an implementation of ECMAScript, the way \"a Toyota Corolla\" is an implementation of a set of car-safety regulations — the regulation and the physical car are different things, and several different physical cars (browsers' engines) all implement the same regulation.</p>\n\n<p><strong>Ecma International</strong> — the standards organisation that owns the ECMA-262 document. It's a Europe-based industry standards body (originally the European Computer Manufacturers Association, hence \"Ecma\") that also standardises other unrelated things. It is not affiliated with any single browser vendor, which matters for the history below.</p>\n\n<p><strong>TC39</strong> — Technical Committee 39, the specific working group inside Ecma International that writes and maintains ECMA-262. When you read \"TC39 proposal,\" it means a change working its way through this committee's process before it becomes part of the official spec. Representatives from Google, Mozilla, Apple, Microsoft, and others all sit on it and have to agree before something ships in the spec.</p>\n\n<p><strong>Specification</strong> — in this context, a precise, prose-and-pseudocode document that says exactly what a piece of software must do, without saying how to build it. ECMA-262 defines, line by line, what happens when you write <code>1 + \"1\"</code>, what a <code>for</code> loop must do, what a <code>Promise</code> must guarantee — but it never says \"and then send these bytes over a wire\" or \"and then light up these pixels,\" because that's not the language's job, as this whole node exists to establish.</p>\n\n<p><strong>Engine</strong> (or JavaScript engine) — a real piece of software that reads ECMAScript-conformant source code and actually executes it: parses the text, works out what it means, and runs it. V8 (used in Chrome and Node.js), SpiderMonkey (Firefox), and JavaScriptCore (Safari) are the three that matter today. Each is written by a different team, in a different language (V8 and JavaScriptCore are C++, for instance), and each is free to implement the spec's rules however it likes internally — the spec constrains observable behaviour, not implementation technique. Two engines can differ wildly in how fast they run the same code while agreeing on every observable result, because \"how fast\" isn't part of what the spec governs.</p>\n\n<p><strong>Host</strong> (or host environment) — a program that embeds one of these engines and hands it extra capabilities the engine alone doesn't have. This is the central term of the whole node and gets its own section below, because understanding what it actually means — not just that it exists — is the entire point.</p>\n\n<p><strong>Host object / host-defined behaviour</strong> — the spec's own vocabulary for \"a thing I am deliberately not defining, because whoever embeds me will define it.\" This isn't an accident of the spec being incomplete — it's a deliberate, named escape hatch built into ECMA-262 from its very first edition.</p>\n\n<p><strong>Realm</strong> — the spec's technical term for one complete, isolated \"world\" a piece of JavaScript code runs inside: its own global object, its own set of built-in objects like <code>Array</code> and <code>Object</code>, all fresh and separate from any other realm. You'll meet this again if you ever deal with iframes (each iframe is a separate realm, which is why an array created in one iframe fails an <code>instanceof Array</code> check performed in a different iframe — two different <code>Array</code> constructors exist, one per realm). For this node, the important fact is simpler: a host is the thing responsible for creating a realm and deciding what extra objects live in it.</p>\n\n<p><strong>Global object</strong> — the object that sits at the top of every realm, holding everything available without importing or requiring it. In a realm the ECMAScript spec builds entirely by itself, the global object would hold only spec-defined things: <code>Object</code>, <code>Array</code>, <code>Math</code>, <code>JSON</code>, <code>Promise</code>, and so on. Anything else that shows up on it — <code>document</code>, <code>fetch</code>, <code>process</code>, <code>Deno</code> — was put there by a host, after the engine finished setting up the spec-defined parts.</p>\n\n<p><strong>Standard library</strong> — a slight false friend worth naming, because languages like Python or Java ship one, and JavaScript's situation genuinely differs. Python's standard library (file I/O, networking, threading) ships with the Python interpreter itself, defined by the Python language project. JavaScript deliberately has no equivalent bundled with the language. What looks like a standard library in a browser (<code>fetch</code>, <code>localStorage</code>) or in Node (<code>fs</code>, <code>http</code>) is host-supplied, not language-supplied, and the two hosts' \"standard libraries\" share almost no API surface, because they were designed independently by different organisations for different purposes.</p>\n\n<h3>The history — why this exists at all</h3>\n\n<p>In May 1995, Brendan Eich was hired at Netscape Communications, the company that made the dominant web browser of the era, Netscape Navigator. He was given an extraordinarily tight brief: build a scripting language that could run inside the browser, and have a working version ready in about ten days, because Netscape 2.0 was already being prepared for release and marketing wanted a story about the browser being \"programmable.\" He wrote the first working version in that window. It went through a few names before landing on the one that stuck — internally called Mocha, briefly shipped as LiveScript, then renamed JavaScript specifically to piggyback on the buzz around Sun Microsystems' unrelated Java language, which was fashionable at the time. The two languages share almost nothing beyond some surface syntax borrowed from C; the shared name was a marketing decision, not a technical relationship, and it has caused confusion for three decades since.</p>\n\n<p>The problem Eich was actually solving was narrow and concrete: before this, making a web page do anything beyond display static content required a full round trip to a server. Validating that a form field wasn't empty, showing or hiding part of a page, reacting to a mouse movement — every one of these meant sending a request over the network (often over a dial-up modem, at the time) and waiting for an entire new page back. Netscape wanted a lightweight language that could run directly inside the browser and manipulate the page the user was already looking at, without that round trip. That's the shape of the very first host: Netscape Navigator 2, embedding this brand-new engine, and handing it exactly the capabilities a browser needed to be useful — reading and writing the parts of a page, popping an <code>alert</code> box, reacting to a click.</p>\n\n<p>The language succeeded immediately and chaotically. Microsoft, needing Internet Explorer to compete, reverse-engineered Netscape's language rather than licensing it, and shipped their own implementation in Internet Explorer 3 in 1996, calling it JScript — a different name specifically to sidestep any trademark claim from Netscape/Sun over \"JavaScript.\" JScript and Netscape's JavaScript were close, but not identical, and they diverged further with each release, since neither vendor was working from any shared written specification — only from Netscape's original implementation as the de facto reference. Web developers were left writing code littered with browser-detection branches just to work around behavioural differences between the two.</p>\n\n<p>This divergence is what forced standardisation. In November 1996, Netscape submitted the language to Ecma International specifically to get an implementation-independent written specification that Microsoft, Netscape, and anyone else could be held to — a document neither company controlled unilaterally. That effort produced ECMA-262, first published in June 1997, and its language name inside the document was deliberately neutral: ECMAScript, avoiding both \"JavaScript\" (Netscape/Sun's trademark) and \"JScript\" (Microsoft's). That is the entire reason two different names exist for what is, for almost every practical purpose, the same language — one is the brand, the other is the standard the brand implements.</p>\n\n<p>Here is the detail this node exists to surface: from that very first 1997 edition, the specification explicitly declined to define any I/O. It defined values, expressions, functions, objects, and how to evaluate a program written using them — but it built in an explicit concept of \"host objects,\" things the spec says exist and behave a certain way only because whatever environment embeds the language chooses to provide them. This wasn't an oversight or something added later under pressure. It was the design, from day one, because the same language needed to be usable inside Netscape's browser, inside Microsoft's browser, and — Netscape had ambitions here too — potentially inside entirely different products with entirely different needs, like server software. A specification that hard-coded \"here is how you talk to a browser's screen\" would have made the language useless anywhere else. Leaving I/O undefined, and building an explicit extension point for hosts to fill in, is what let the exact same language later run inside Node.js in 2009 — a context with files and network sockets and no screen at all — without changing a single rule in ECMA-262 to make that possible.</p>\n\n<h3>The mechanism — traced</h3>\n\n<p>Concretely, here is what happens when a JavaScript engine is asked to run a program, and exactly where the boundary this node is about actually sits.</p>\n\n<p>An engine is handed a string of source text — your code. The engine's first job, entirely specified by ECMA-262, is <strong>parsing</strong>: turning that text into a structured internal representation the engine can act on (in practice, most engines eventually produce bytecode, then further optimise hot code into machine code — but none of that internal detail is what the spec constrains; the spec constrains observable results, not internal representations). The engine's second job is <strong>evaluation</strong>: walking through that structure and carrying out the operations it describes, according to rules the spec writes down in extraordinary, almost pedantic detail. For example, ECMA-262 spells out, as a sequence of numbered abstract steps, exactly what happens when you write <code>1 + \"1\"</code> — how each operand is converted, in what order, to what intermediate type, before the final concatenation happens and produces the string <code>\"11\"</code>. Every quirky JavaScript coercion rule you've ever hit exists because some specific numbered algorithm in this document says so, not because an engine improvised.</p>\n\n<p>Evaluation happens inside a realm, and the realm's global object is populated in two phases. First, the engine itself creates and installs everything the spec actually defines — the <code>Object</code> constructor, <code>Array</code>, <code>Math</code>, <code>JSON</code>, <code>Promise</code>, and every other built-in ECMA-262 specifies, plus the global functions the spec itself defines, like <code>parseInt</code> and <code>encodeURIComponent</code>. This phase is identical no matter what host is running the engine, because it's pure language, governed entirely by ECMA-262 with zero host involvement. Second — and this is the phase that actually matters for this node — the host, before or as it lets your code run, adds its own objects and functions onto that same global object. A browser's host code adds <code>document</code>, <code>fetch</code>, <code>setTimeout</code>, <code>localStorage</code>, and hundreds more. Node's host code instead adds things like <code>process</code>, <code>require</code>, and a <code>global</code> object carrying its own different set of capabilities, defined by an entirely separate specification process (Node's APIs are documented by the Node.js project itself, following none of TC39's process, since they were never a TC39 concern to begin with).</p>\n\n<p>This is directly demonstrable, and worth actually trying rather than taking on faith. Most JavaScript engines can be embedded with essentially no host code attached at all — this is exactly what a \"sandboxed\" or \"isolated\" JavaScript environment is, and small embeddable engines built for exactly this purpose exist (QuickJS is a commonly cited one). Run a program in an environment like that, and <code>typeof console</code> comes back <code>\"undefined\"</code>, <code>typeof setTimeout</code> comes back <code>\"undefined\"</code>, and there is no way to make the program produce any output an outside observer could see — its return value can only be read by whatever embedded it, because nothing about \"showing something to a person\" was ever the language's job. Meanwhile <code>typeof Array</code>, <code>typeof JSON</code>, and <code>typeof Promise</code> all still come back correctly, because those genuinely are language-level, defined by ECMA-262 itself, requiring no host cooperation whatsoever.</p>\n\n<p>Put the two phases together and the whole node compresses into one operational fact: <strong>the global object in any running JavaScript program is language-defined built-ins plus whatever a specific host chose to bolt on</strong>, and every capability you've ever associated with \"JavaScript\" that isn't in the first category came from the second. <code>console.log</code>, despite being the very first thing almost anyone types when learning the language, is entirely host-supplied — both the browser and Node happen to provide something called <code>console</code> with a method called <code>log</code>, but they are two unrelated implementations that happen to share a name and a rough shape, maintained by entirely different projects, and a hypothetical third host is under no obligation to provide either.</p>\n\n<h3>What an interviewer does with this</h3>\n\n<p>\"Is JavaScript single-threaded?\" is a very common opener, and a strong answer starts exactly here rather than jumping straight to the event loop: the language specification doesn't mention threads at all — it's silent on concurrency entirely — so \"single-threaded\" is a property of specific hosts' typical configuration (a browser's page, or a default Node process), not a property of the language itself. Node can run multiple threads via its <code>worker_threads</code> module, and a browser page can run genuinely parallel code via Web Workers; both are true precisely because threading was left to hosts to decide, exactly like I/O was.</p>\n\n<p>\"What's the difference between JavaScript and ECMAScript?\" is asked almost verbatim, and the strong answer is the standards-history one above, in miniature: ECMAScript is the specification (ECMA-262, maintained by TC39 at Ecma International); JavaScript is the common name for engines that implement it, and the two names exist specifically because of the Netscape/Microsoft trademark situation in the mid-1990s, not because they refer to different things technically.</p>\n\n<p>\"Why doesn't <code>window</code> exist in Node.js?\" is a weaker-sounding but genuinely diagnostic question — a weak answer says \"because Node is different\"; a strong answer says <code>window</code> was never part of the language to begin with, it's the name the browser host happens to give its global object, and Node's host code populates the same underlying global-object mechanism with an entirely different set of names (<code>global</code>, <code>process</code>) because it's solving a different problem with no concept of a window at all.</p>\n\n<p>\"What makes code 'isomorphic' or 'universal' — able to run in both the browser and Node?\" is the practical payoff of this whole node: code is portable exactly to the extent that it only touches things ECMA-262 itself defines (arithmetic, <code>Array</code> methods, <code>Promise</code>, plain objects) and avoids anything either host bolted on. The moment code touches <code>document</code> or <code>fs</code>, it has silently taken a dependency on one specific host's extension of the language, and it will throw a reference error the instant it's run somewhere that host's objects don't exist.</p>\n"
    },
    {
      "anchor": "node-1-how-a-page-becomes-a-running-program",
      "title": "Node 1 — How a page becomes a running program",
      "content": "<h3>Where this sits</h3>\n\n<p>Node 0 established that JavaScript has no capabilities of its own and that everything actually useful comes from a host. This node is the browser acting as that host for the very first time: taking a URL, getting bytes back, and getting from those bytes to your code actually running against something. Every later node assumes you understand two things this node builds — that the DOM is a separate, live thing the browser constructs and not the HTML text itself, and that when your script runs is a decision the browser makes, not a fixed moment.</p>\n\n<h3>Every term, from absolute zero</h3>\n\n<p><strong>URL</strong> — Uniform Resource Locator, the address typed into a browser or clicked as a link. For this node, the only relevant fact is that it names a network request the browser is about to make; how that request actually reaches a server is the <code>http</code> and <code>dns</code> topics' territory, not this one.</p>\n\n<p><strong>HTML</strong> — HyperText Markup Language, the text format a server sends back describing a page's content and structure using tags like <code>&lt;p&gt;</code>, <code>&lt;div&gt;</code>, <code>&lt;script&gt;</code>. It is plain text, byte for byte, exactly like a <code>.txt</code> file — nothing about it is inherently a live object you can manipulate. That distinction is the entire point of this node.</p>\n\n<p><strong>Parser</strong> — the piece of the browser that reads a text format left to right and decides what it means. An HTML parser reads HTML text and, per tag, decides \"this opens a paragraph,\" \"this closes a division,\" \"this is a script.\" Parsing is not unique to browsers — every compiler and interpreter parses source text — but the HTML parser specifically is what turns page text into structure.</p>\n\n<p><strong>DOM (Document Object Model)</strong> — the live, in-memory tree of objects the browser builds while parsing HTML: one object per tag, nested exactly the way the tags were nested in the text. Once built, this tree is what JavaScript actually reads and writes, and what the browser draws to the screen from. Note precisely what \"DOM\" is not: it is not the HTML text, and it is not saved to disk anywhere — it's constructed fresh in memory every time a page loads, and mutating it never touches whatever file or response originally produced it.</p>\n\n<p><strong>Node (DOM sense)</strong> — one single object in that tree — one paragraph, one image, one span of text. This is an unfortunate but unavoidable name collision worth flagging explicitly: a \"DOM node\" (one element or piece of text in the tree) has nothing to do with \"Node.js\" (the separate server-side host from Node 0) or \"a node in this landscape's causal chain\" — three completely unrelated uses of the same English word that you will see used interchangeably in different contexts without anyone flagging the collision.</p>\n\n<p><strong>Element</strong> — the specific kind of DOM node that corresponds to a tag, like <code>&lt;div&gt;</code> or <code>&lt;p&gt;</code>, as opposed to a text node (a node holding plain text with no tag) or other less common node types.</p>\n\n<p><strong><code>&lt;script&gt;</code></strong> — the HTML tag that embeds or references JavaScript source code inside a page. It can either contain the code directly between opening and closing tags, or point at an external file via a <code>src</code> attribute, exactly like an <code>&lt;img src=\"...\"&gt;</code> points at an external image file rather than embedding image data inline.</p>\n\n<p><strong>Synchronous</strong> — happening immediately, in order, with nothing else able to proceed until it finishes. Reading a script tag's source and running it top to bottom, with the parser waiting, is a synchronous operation in exactly this sense; you'll see this word again heavily once the event loop (Node 4) formalises it.</p>\n\n<p><strong>Parser-blocking</strong> — the specific, named behaviour where the HTML parser stops parsing entirely while a synchronous script downloads and runs, then resumes afterward. This is a property of the default, unattributed <code>&lt;script&gt;</code> tag, not of scripts in general — the attributes below exist precisely to opt out of it.</p>\n\n<p><strong><code>defer</code></strong> — a boolean attribute you add to a script tag (<code>&lt;script defer src=\"...\"&gt;</code>) telling the browser: fetch this in the background while parsing continues, but hold off running it until the entire document has been parsed, and if there are several deferred scripts, run them in the exact order they appear in the document.</p>\n\n<p><strong><code>async</code></strong> — a different boolean attribute telling the browser: fetch this in the background, and the instant it finishes downloading, run it immediately — even if that means interrupting the parser mid-document, and with no guarantee about ordering relative to other scripts.</p>\n\n<p><strong><code>type=\"module\"</code></strong> — an attribute value that changes what kind of JavaScript file the browser thinks it's loading: not a plain script, but an ES module, meaning it may use <code>import</code>/<code>export</code> syntax and gets its own module-scoped behaviour, covered next.</p>\n\n<p><strong>Module</strong> — a single JavaScript file treated as a self-contained unit with its own private scope by default (nothing inside it leaks into the global object unless explicitly exported), which can declare what it makes available to other files (<code>export</code>) and what it needs from other files (<code>import</code>).</p>\n\n<p><strong><code>import</code> / <code>export</code></strong> — the syntax a module uses to declare its dependencies (<code>import</code>) and its public surface (<code>export</code>). When a browser sees an <code>import</code> statement, it turns the imported specifier into a URL, fetches that URL as its own separate module, and evaluates it exactly once, caching the result by that resolved URL so a module imported from five different places is still only fetched and run a single time.</p>\n\n<p><strong>Global object</strong> — carried over from Node 0: the top-level object holding everything available without an import, populated first by the language, then extended by the host.</p>\n\n<p><strong><code>window</code></strong> — the specific name the browser host gives the global object inside a page. Whenever you see a bare, undeclared identifier resolve successfully in browser code — <code>document</code>, <code>fetch</code>, <code>alert</code> — you are reading a property that lives on <code>window</code>, whether or not the code writes <code>window.</code> in front of it.</p>\n\n<p><strong><code>globalThis</code></strong> — a single spec-level name, added to the language itself specifically so code doesn't have to know or care which host it's running in just to find the global object. It resolves to <code>window</code> in a browser page, <code>self</code> inside a Web Worker (Node 9), and <code>global</code> inside Node.js — three different host-chosen names, unified under one language-level alias.</p>\n\n<p><strong>Event</strong> — a signal the browser dispatches to say \"something happened,\" which code can subscribe to and be called back for. This node uses two specific, named events without yet explaining the general event mechanism in full — that's Node 2's job — but you need the two names now.</p>\n\n<p><strong><code>DOMContentLoaded</code></strong> — the named event the browser dispatches the moment the entire HTML document has been parsed into a complete DOM tree and every deferred script has finished running, but before things like images necessarily finish downloading.</p>\n\n<p><strong><code>load</code></strong> — a separate named event, dispatched later, once every subresource the page references — images, stylesheets, fonts, embedded frames — has also finished loading, not just the document's own markup and scripts.</p>\n\n<h3>The history — why this exists at all</h3>\n\n<p>To understand why the parser stops dead for a plain <code>&lt;script&gt;</code> tag, you have to go back to what \"manipulating a page with JavaScript\" actually meant in 1995, at the exact moment Brendan Eich built the language (Node 0's history). The DOM as a rich, general API — <code>document.getElementById</code>, <code>element.appendChild</code>, and so on — did not exist yet; the World Wide Web Consortium didn't publish its first formal DOM specification (DOM Level 1) until October 1998, three years later. What Navigator 2 shipped in 1995 instead was a single, blunt capability: <code>document.write()</code>, a function that literally injects a string of raw HTML text into the document at the exact point the parser currently is. If your script called <code>document.write(\"&lt;p&gt;Hello&lt;/p&gt;\")</code> while the parser was midway through the page, that text was spliced directly into the stream the parser was consuming, as if it had been typed there by the page's author.</p>\n\n<p>That single fact is why parser-blocking exists and has never been safe to remove. If <code>document.write</code> can inject arbitrary new HTML at the parser's current position, then the parser genuinely cannot safely run ahead of a script that hasn't executed yet — it has no way to know whether that script is about to rewrite the very content coming next. The only correct thing to do is stop, run the script to completion, and only then continue parsing whatever the script left behind (either the original markup, or markup the script injected via <code>document.write</code>, or both). This is not a historical accident that a later browser could have simply \"fixed\" — as long as <code>document.write</code> exists and old pages depend on it working exactly this way, an HTML parser cannot get ahead of a synchronous script without risking silently corrupting the page. Browsers still support this today for exactly that reason: breaking it would break a nontrivial fraction of the pre-2000s web that quietly still works.</p>\n\n<p>The <code>defer</code> attribute is old — it appears in the HTML 4.0 specification from December 1997 and Internet Explorer 4 implemented it almost immediately — but for years it was inconsistently implemented and developers didn't trust it, which is the real reason \"put your <code>&lt;script&gt;</code> tag right before <code>&lt;/body&gt;</code>\" became the dominant folk convention for so long: putting the script last, after all the content it needed already existed in the markup above it, sidestepped the parser-blocking problem without needing to rely on an attribute that didn't reliably behave the same way across browsers. <code>async</code> arrived much later, standardised as part of HTML5 in the 2009–2010 era, specifically to solve a different problem: analytics and ad scripts that had no dependency on the page's content and no need to preserve execution order relative to anything else, which <code>defer</code>'s ordering guarantee made unnecessarily restrictive for that use case.</p>\n\n<p><code>type=\"module\"</code> is newer still. The <code>import</code>/<code>export</code> syntax itself was standardised in ECMAScript 2015 (the sixth edition of ECMA-262, informally called ES6) as part of the language, but browsers didn't actually implement native, no-build-tool module loading in a <code>&lt;script&gt;</code> tag until 2017 — Safari 10.1 and Chrome 61 shipped it that year, with Firefox following in 2018. For roughly two years, developers who wanted module syntax had no choice but to use a build tool (Babel, webpack) to convert it into older code first; native support only became reliable to depend on directly in the browser well after that.</p>\n\n<h3>The mechanism — traced</h3>\n\n<p>Follow one page load from the moment the URL is entered, being precise about the order things actually happen.</p>\n\n<p>Before a single byte of the response has even been read, the browser has already created a <strong>realm</strong> for the page (Node 0's term) and populated its global object — <code>window</code> — with everything the ECMAScript language defines plus everything the browser host bolts on: <code>document</code> (already existing, but as an essentially empty document at this point), <code>fetch</code>, <code>setTimeout</code>, and the rest. This setup happens as part of navigation, ahead of parsing the actual page content.</p>\n\n<p>Bytes then arrive from the network as the response body, decoded according to a declared or detected character encoding into actual HTML text. The HTML parser consumes this text incrementally, tag by tag, as it arrives — it does not wait for the entire response to finish downloading before starting; it can begin building the DOM tree from the first bytes that arrive, well before the last ones do, which is one reason a large page can start rendering its top before the rest has finished transferring.</p>\n\n<p>Each opening and closing tag the parser recognises causes it to create, and append to the growing tree, a new DOM node — an element for a tag pair, a text node for the text between them. This is exactly the moment the DOM diverges from the HTML text underneath it: the tree that results is a structured object graph in memory, and once built, the original text it came from is never consulted again by the running page. Every subsequent change JavaScript makes happens to this tree of objects, never to the text.</p>\n\n<p>When the parser's cursor reaches a <code>&lt;script&gt;</code> tag, it must decide what to do based on that tag's attributes, and the three cases genuinely diverge:</p>\n\n<p>With no <code>defer</code> and no <code>async</code>: the parser halts entirely. If the script has a <code>src</code>, the browser fetches that file — blocking further parsing during the download too, not just during execution — then hands the resulting source text to the JavaScript engine, which does exactly what Node 0 described: parses it, then evaluates it, synchronously and to completion, against the realm's already-populated global object. Only once the engine finishes does the HTML parser resume from wherever it left off. If this script tries to read <code>document.body</code>, and it sits above the body's own markup in the document, <code>document.body</code> is simply <code>null</code> at that moment — the tree genuinely hasn't been built that far yet — which is the exact, mechanical reason so much old code either lived at the very bottom of <code>&lt;body&gt;</code>, or wrapped its logic in a <code>DOMContentLoaded</code> listener to defer running it until the tree existed.</p>\n\n<p>With <code>defer</code>: the parser keeps going immediately, and the browser fetches the script file in parallel, in the background, without pausing anything. The engine only actually evaluates it once the entire document has finished parsing — meaning the full DOM tree already exists and <code>document.body</code> is guaranteed non-null — and if several scripts on the page carry <code>defer</code>, the browser guarantees they run strictly in the order they appear in the markup, regardless of which one's network fetch happened to finish first.</p>\n\n<p>With <code>async</code>: the parser also keeps going immediately and the file downloads in parallel, but the moment that download finishes — which could be while the parser is still halfway through the document — the engine interrupts and runs it right then, with no ordering guarantee relative to any other script on the page. Two <code>async</code> scripts can run in either order, purely depending on which network request happens to complete first, which is exactly why <code>async</code> is only appropriate for code with genuinely no dependency on anything else on the page.</p>\n\n<p>With <code>type=\"module\"</code>: parsing continues in parallel exactly like <code>defer</code>, and execution is likewise deferred until the document is fully parsed. The extra step is dependency resolution: before the module can run, the browser must resolve every <code>import</code> specifier inside it into a URL, fetch that URL (which might itself contain further <code>import</code> statements, recursively), and evaluate each unique resolved URL's module code exactly once — caching by that URL, so five different files importing the same shared utility module all receive the same single evaluated instance of it, not five separate copies.</p>\n\n<p>Once the parser finishes consuming the entire document and every deferred/module script has finished evaluating, the browser dispatches <code>DOMContentLoaded</code> against <code>document</code>. Any code that registered a listener for it (using the general event-listener mechanism Node 2 will explain properly) runs at that moment, with a guarantee that the full DOM tree is available to query and modify safely. Separately, and later, once every image, external stylesheet, font file, and embedded frame the page references has also finished loading — work that can continue well after the markup itself was fully parsed — the browser dispatches <code>load</code> on <code>window</code>. Code needing an accurate image dimension, for instance, must wait for this second, later event; asking before it fires can return a size of zero, because the image genuinely hasn't finished arriving yet.</p>\n\n<h3>What an interviewer does with this</h3>\n\n<p>\"What's the difference between the DOM and HTML?\" sounds almost too basic to be asked seriously, and yet a genuinely precise answer — HTML is text that arrives once and is discarded, the DOM is a live object tree the browser builds from it and continuously renders from thereafter, and the two routinely diverge the instant any JavaScript runs — is rarer than it should be, and immediately signals real understanding versus rote familiarity.</p>\n\n<p>\"Why would you put a script tag at the bottom of the body instead of the top?\" is really asking whether you understand parser-blocking without necessarily naming it: a strong answer explains that an unattributed script at the top halts parsing of everything below it, producing a blank page while it downloads and runs, and that placing it at the bottom (or, better, using <code>defer</code>) avoids that without needing to explain browser internals from scratch.</p>\n\n<p>\"When would you choose <code>async</code> over <code>defer</code>?\" tests whether you know these are not interchangeable performance toggles but solve genuinely different problems: <code>defer</code> for anything depending on document order or wanting the DOM guaranteed complete first (your own app code), <code>async</code> only for independent scripts where execution order genuinely doesn't matter (most third-party analytics/ad tags).</p>\n\n<p>\"What's the difference between <code>DOMContentLoaded</code> and <code>load</code>?\" is asked constantly and a weak answer conflates them; a strong one gives the precise boundary — DOM-complete-and-scripts-done versus every-subresource-also-done — and can name a concrete situation each is correct for (attaching event listeners to elements versus measuring a loaded image's actual dimensions).</p>\n"
    },
    {
      "anchor": "node-2-the-runtime-calls-you-back",
      "title": "Node 2 — The runtime calls you back",
      "content": "<h3>Where this sits</h3>\n\n<p>Node 1 got a script running once, against a complete DOM. This node is what turns a page from \"runs once and stops\" into \"keeps responding for as long as it's open\" — and it introduces the callback shape that Node 4's event loop, Node 5's timers, and Node 6's <code>fetch</code> are all just further variations of. The propagation model and user activation, specifically, get referenced by name repeatedly in every node from here on.</p>\n\n<h3>Every term, from absolute zero</h3>\n\n<p><strong>Callback</strong> — a function you hand to something else, with the understanding that the other party will call it later, at a time of its own choosing, rather than you calling it yourself right now. Handing over a function like this — rather than the function running immediately — is sometimes called <strong>inversion of control</strong>: control over <em>when</em> your code runs has been handed to whatever you gave the callback to.</p>\n\n<p><strong>Listener</strong> — the specific term for a callback registered against an event; \"event listener\" and \"callback\" are used near-interchangeably in this context, but \"listener\" specifically implies it's attached to listen for a named event on a particular element.</p>\n\n<p><strong><code>addEventListener(type, handler, options)</code></strong> — the method that registers a listener. <code>type</code> is a string naming the event (\"click\", \"input\", \"keydown\"). <code>handler</code> is the callback function. <code>options</code> is an optional object whose <code>capture</code> field is explained below.</p>\n\n<p><strong>DOM Level 0 event handling</strong> — the earlier, more primitive way of attaching a handler, either as an HTML attribute (<code>&lt;button onclick=\"doThing()\"&gt;</code>) or as a direct property assignment in JavaScript (<code>button.onclick = doThing</code>). Both exist and both still work, but both share a hard limitation this node's history explains: an element can only hold <em>one</em> handler per event this way, because assigning to <code>.onclick</code> a second time simply overwrites the first.</p>\n\n<p><strong>Event object</strong> — a single object the browser constructs and passes as the first argument to your handler, describing the event: what kind it was, where it happened, and providing methods to influence what happens next. Every handler function receives one, conventionally named <code>event</code> or <code>e</code>.</p>\n\n<p><strong><code>event.target</code></strong> — a property on that object naming the exact DOM element the event actually originated from — the specific <code>&lt;li&gt;</code> that was clicked, say, even if the listener handling it is attached somewhere else entirely.</p>\n\n<p><strong><code>event.currentTarget</code></strong> — a different property, naming the element whose listener is executing <em>right now</em>. These two are identical only when the listener is attached directly to the exact element the user interacted with; the moment a listener is attached higher up the tree, they diverge, and that divergence is what makes delegation (below) possible at all.</p>\n\n<p><strong>Event propagation</strong> — the defined path an event travels through the DOM tree before and after reaching its target, explained in full in the mechanism section below. It has three named phases.</p>\n\n<p><strong>Capture phase</strong> — the first phase: the event travels <em>downward</em>, starting from the top of the document, through each ancestor, toward the element it actually happened on.</p>\n\n<p><strong>Target phase</strong> — the moment the event is being handled directly on the element it happened on.</p>\n\n<p><strong>Bubble phase</strong> — the phase after that: the event travels back <em>upward</em>, from the target element, back out through each ancestor, all the way to the top again.</p>\n\n<p><strong>Delegation</strong> — a pattern, not a separate API: attaching one single listener to a stable parent element, and using <code>event.target</code> inside that one handler to work out which of many possible children was actually interacted with, rather than attaching a separate listener to every child individually.</p>\n\n<p><strong>Default action</strong> — a behaviour the browser itself performs, natively, in response to certain events, independent of any JavaScript: following a clicked link, submitting a clicked form's data, toggling a clicked checkbox, scrolling when the space bar is pressed. These happen because the browser is built to do them, not because any listener made them happen.</p>\n\n<p><strong><code>event.preventDefault()</code></strong> — a method on the event object that cancels whichever default action the browser would otherwise have performed for this specific event, without affecting whether the event keeps travelling to other listeners.</p>\n\n<p><strong><code>event.stopPropagation()</code></strong> — a different method that halts the event's further travel through capture/bubble phases — no listener further along the path will be called — without affecting whether the browser's own default action still happens.</p>\n\n<p><strong>User gesture</strong> — an action the browser can attribute directly to the user doing something physical: a genuine click, a keypress, a tap. Code calling a function itself, or a timer firing on its own, is explicitly not a user gesture, even if that code was originally set up in response to one.</p>\n\n<p><strong>User activation</strong> — a flag the browser attaches to the current moment when a user gesture has just occurred, used as a permission check by certain powerful APIs before they'll act. It has two forms worth distinguishing: <strong>transient activation</strong> is short-lived (on the order of a few seconds) and gets consumed — spent — the first time some gesture-gated API actually uses it; <strong>sticky activation</strong> is a separate, longer-lived flag that, once set by any gesture at all, persists for the rest of that page's lifetime and is used by a smaller number of checks (like whether autoplay-with-sound has ever been permitted on this page) that don't need to be re-earned by every single action.</p>\n\n<h3>The history — why this exists at all</h3>\n\n<p>The propagation model — capture, then target, then bubble — looks deliberately over-engineered until you know it's the result of a genuine standards fight, not a single team's considered design.</p>\n\n<p>By the mid-to-late 1990s, both major browsers needed a way for JavaScript to react to user interaction, and they built two philosophically opposite models with no coordination between them. Netscape Navigator 4 (1997) implemented what it called <strong>event capturing</strong>: an event started at the top of the document and travelled downward toward the element it happened on, and code higher up the tree could intercept it before it ever reached its target. Microsoft's Internet Explorer 4, shipped the same year, went the opposite direction entirely, implementing only <strong>event bubbling</strong>: an event started exactly at the element it happened on and then travelled outward, upward, through each ancestor. Code written against one browser's model frequently did not work at all in the other — not a minor compatibility wrinkle, but two irreconcilable mental models of \"which way does an event go.\"</p>\n\n<p>The W3C's Document Object Model Level 2 Events specification, published as a formal recommendation in November 2000, resolved this not by declaring a winner but by keeping both, sequenced: an event now travels down first (satisfying Netscape's original capturing model), reaches the target, then travels back up (satisfying Microsoft's bubbling model). This is a compromise in the most literal sense — it exists in its exact three-phase shape specifically because neither vendor's browser could be made to lose entirely, and every browser shipped since has implemented this same three-phase sequence as the actual standard, which is why <code>addEventListener</code>'s capture option still exists today: it's a fossil of the older capturing-only model, preserved as an opt-in rather than removed, because DOM Level 2 kept both philosophies alive rather than discarding one.</p>\n\n<p><code>addEventListener</code> itself is part of that same 2000 specification, and it was introduced to fix a real, structural limitation of the DOM Level 0 handler-property style that came before it: assigning <code>button.onclick = handlerA</code> and later <code>button.onclick = handlerB</code> in the same codebase doesn't register two handlers — the second assignment silently overwrites the first, because <code>.onclick</code> is just an ordinary object property that can only hold one value at a time. As pages grew more complex and multiple independent pieces of code legitimately wanted to react to the same event on the same element — a piece of analytics code and the actual UI logic, say — this became a genuine architectural constraint, not a stylistic preference. <code>addEventListener</code> solved it by maintaining an internal list per element per event type, onto which any number of callers can each append their own listener without disturbing anyone else's.</p>\n\n<p>User activation is much newer and reflects a completely different kind of history: an escalating arms race between advertisers and users, not a browser-vendor dispute. Pop-up windows, autoplaying audio, and clipboard access were each, in turn, abused so aggressively by intrusive advertising through the 2000s that every major browser independently began restricting them to situations directly triggered by a real click — and the rule that a gesture's permission is transient and spent quickly, rather than granted indefinitely, exists specifically to close the loophole of a page waiting until well after a legitimate click to abuse the permission that click nominally justified.</p>\n\n<h3>The mechanism — traced</h3>\n\n<p>Follow one concrete click precisely, from the physical action to the last listener called.</p>\n\n<p>The user clicks somewhere on the page. The browser performs what's called hit testing — using the click's pixel coordinates to work out exactly which single DOM element is visually at that point (the deepest, most specific one — if a <code>&lt;span&gt;</code> inside a <code>&lt;button&gt;</code> inside a <code>&lt;form&gt;</code> is what's actually under the cursor, the span is the target, not the button or the form, even though visually you might describe yourself as \"clicking the button\"). This element becomes <code>event.target</code> for the entire dispatch that follows, fixed and unchanging no matter how far the event later travels.</p>\n\n<p>The browser then computes the full propagation path: the chain of ancestors from the very top of the document (conceptually starting at <code>window</code>, then <code>document</code>, then the <code>&lt;html&gt;</code> element, and so on down) all the way to the target element itself.</p>\n\n<p>Dispatch proceeds in the three phases, strictly in this order. First, the <strong>capture phase</strong>: starting from the top of that path and moving downward, at each ancestor element in turn, the browser checks whether any listener was registered on that element with <code>{capture: true}</code>, and if so, calls it, setting <code>event.currentTarget</code> to that ancestor for the duration of the call. Most listeners in ordinary code are not registered this way, so in practice this phase usually calls nothing at all, but it genuinely happens, in this order, every single time.</p>\n\n<p>Second, the <strong>target phase</strong>: once the walk reaches the target element itself, any listeners registered directly on it are called — both capturing and non-capturing listeners registered on the target itself fire here, since target-phase listeners don't distinguish the two.</p>\n\n<p>Third, the <strong>bubble phase</strong>: the walk now reverses, moving back up from the target toward the top of the document, and at each ancestor, any listener registered <em>without</em> <code>{capture: true}</code> (the default, and by far the most common case) is called, again with <code>event.currentTarget</code> set to whichever ancestor is currently being visited.</p>\n\n<p>At every single one of these calls, throughout all three phases, <code>event.target</code> never changes — it's fixed at dispatch time to the exact element that was actually clicked — while <code>event.currentTarget</code> changes at each step to reflect whichever element's listener is presently executing. This is precisely the mechanism delegation exploits: attach one listener during the bubble phase to a stable container far up the tree, and inside that single handler, read <code>event.target</code> to discover which specific descendant the click actually originated from — a descendant that might not even have existed in the DOM at the time the listener was originally registered, since the browser resolves <code>event.target</code> fresh, from the current DOM, at the moment of each new click, not from whatever existed when <code>addEventListener</code> was first called.</p>\n\n<p>After every listener along the entire path has been given its chance to run, the browser checks whether <code>preventDefault()</code> was called by any of them at any point during dispatch. If it was, the native default action tied to this specific element-and-event combination is skipped entirely — the link is not followed, the form is not submitted. If <code>stopPropagation()</code> was called instead, that's a completely separate check applied during the walk itself: the moment it's called, the browser simply stops moving to the next ancestor (or, in the capture phase, the next descendant) — no further listeners anywhere else on the path are invoked — but this has no bearing whatsoever on whether the default action still happens afterward, because that check is independent of propagation entirely. This is exactly why the two are frequently confused and why mixing them up produces a real, specific bug: calling only <code>stopPropagation()</code> on a submit button's click, expecting it to also stop the form submitting, leaves the form submitting normally, because stopping propagation and cancelling the default action were never the same operation.</p>\n\n<p>User activation works as a flag the browser maintains internally, tied to the current execution context, updated the instant a genuine user gesture (click, key press, touch) is what caused the currently-running code to be running at all. Certain APIs — opening a new window, writing to the clipboard, requesting fullscreen, playing audio with sound — check this flag synchronously, at the exact moment they're called, and refuse to act if it isn't set. Because the flag is transient, and because <code>await</code>ing a promise (Node 4's mechanism) hands control back to the event loop and lets other, unrelated code run before your function resumes, code that calls one of these APIs after an <code>await</code> inside a click handler is frequently refused — by the time execution resumes, enough of the transient window has typically elapsed, or an intervening task has already consumed it, that the flag is simply no longer set when the gesture-gated call finally happens.</p>\n\n<h3>What an interviewer does with this</h3>\n\n<p>\"Given three nested divs, each with both a capturing and a bubbling click listener, what order do they fire in if the innermost one is clicked?\" is a direct test of whether you can trace the mechanism rather than recite the three phase names. A strong answer walks it exactly as above: all the capturing listeners fire first, outermost to innermost, then the innermost element's own listeners (both kinds) fire, then all the bubbling listeners fire, innermost to outermost — six calls total in a specific, derivable order, not something to memorise as a table.</p>\n\n<p>\"How would you implement a to-do list where clicking 'delete' on any item — including ones added after the page loaded — removes that item, using only one event listener?\" is asking for delegation, specifically probing whether you understand that <code>event.target</code> is resolved fresh at click time against whatever the current DOM actually contains, which is exactly why one listener on the list's container correctly handles items that didn't exist when that listener was first attached.</p>\n\n<p>\"A form has a submit button, and a click listener on it calls only <code>event.stopPropagation()</code>, expecting to prevent the form from submitting. Does it work?\" is a very common trap question, and the correct answer is no — the form still submits, because stopping propagation and cancelling the browser's default action are two independent mechanisms, and only <code>preventDefault()</code> touches the latter.</p>\n\n<p>\"Why does <code>window.open()</code> sometimes get silently blocked as a popup when called from inside an async function, even though the function was triggered by a real click?\" tests whether you connect user activation forward to the event loop: a strong answer explains that an <code>await</code> earlier in that function returns control to the event loop, and by the time execution resumes afterward, the transient activation window from the original click has typically already expired, so the later call is being evaluated with no active user gesture behind it at all.</p>\n"
    }
  ]
}
