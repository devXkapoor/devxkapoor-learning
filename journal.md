# Mastery Track Journal

Running log across all topic conversations. Each topic conversation appends one entry here at the end of its session.

Format per entry:

```
## YYYY-MM-DD — <topic-slug>
- Status: not-started | in-progress | stalled | done
- What was covered:
- What's left:
- Notes:
```

---

## Log

(entries will be appended here as topics progress)

## 2026-08-27 — javascript
- **State:** in-progress
- **Project:** `shortlist` — spec written (`topics/javascript/project-spec.md`), build not started
- **Covered:** landscape (19 nodes), both banks (184 recall + 62 prep), **all 18
  elaborations**, project spec, and Resolution 1 of a new Discuss track.
- **Left:** Resolutions 2–6; the `shortlist` build; the remaining ~120 recall questions.
- **Notes:** He read 63 recall questions and left 64 follow-ups (~25,800 words), 40 of
  which self-identify as repeats of earlier doubts. That reframed the work: rather than
  answering per question, resolutions are written per *root gap* — six of them — each
  naming which follow-ups it closes. Also a large amount of meta-repo UX work this
  session: question markers, follow-up notes, export/restore, collapsible disclosure
  blocks, grouped decks, a theme rebuild, and asset cache-busting. Two self-inflicted
  regressions (a 965-line deletion caught before push; three CSS rules deleted and
  shipped) led to guards being added: an unstyled-class check and `bump-assets.py`.
