{
  "sections": [
    {
      "anchor": "overview",
      "title": "Overview",
      "content": "<p><strong>Project repo:</strong> <a href=\"#\">not yet created — see project spec once we build it</a></p>"
    },
    {
      "anchor": "node-0-the-problem-that-exists-before-git-does",
      "title": "Node 0 — The problem that exists before Git does",
      "content": "<p>You have a folder of files. It changes over time. Three things go wrong immediately, and every feature in Git is a response to one of them.</p>\n      <p>The first is that you lose the past. You edit a file, the edit is bad, and the version that worked is gone. The obvious fix is to copy the folder before editing — <code>project-old</code>, <code>project-backup</code>, <code>project-final-v2</code>. This works for about a week, and then you have eleven folders, no idea which is authoritative, no record of what changed between any two of them, and no record of why anything changed.</p>\n      <p>The second is that you can't work on two things at once. If you're halfway through a risky change and something urgent comes up, your options are to finish the risky change first, or to abandon it, or to make yet another folder copy. All three are bad.</p>\n      <p>The third is that you can't combine work with another person. If two people each edit their own copy of the folder, there's no mechanical way to produce a folder containing both sets of edits. Someone has to sit down and manually reconcile the files, and if both edited the same file, someone has to decide line by line.</p>\n      <p>Everything below is a chain of solutions to these three, and each solution creates the conditions for the next problem.</p>"
    },
    {
      "anchor": "node-1-save-whole-snapshots-addressed-by-their-content",
      "title": "Node 1 — Save whole snapshots, addressed by their content",
      "content": "<p><strong>The limitation:</strong> copies of a folder are unnamed, undated, unexplained, and duplicated wholesale on disk.</p>\n      <p>The first move is to stop copying folders manually and let a program keep the copies, in one hidden place, permanently. That place is <code>.git/</code>, and specifically <code>.git/objects/</code>. When you record a version, Git stores the state of every file at that instant. Not the changes — the whole state.</p>\n      <p>The interesting decision is how the stored things get named. Git doesn't number them. It runs the content through a hash function and uses the resulting forty-character string as the name. This has two consequences that matter enormously downstream: deduplication (identical content always hashes to the same name, so it's never stored twice) and integrity (tampered content produces a mismatched hash, so tampering is detectable).</p>\n      <ul>\n        <li><strong>Blob</strong> — the contents of one file, and only the contents; no filename, no path, no permissions.</li>\n        <li><strong>Tree</strong> — a directory listing: a sorted set of (mode, type, hash, name) entries pointing at blobs and other trees.</li>\n        <li><strong>Commit</strong> — a pointer to one tree, zero or more parent commits, author, committer, message.</li>\n      </ul>\n      <p>Because filenames live in trees rather than blobs, renaming a file creates a new tree but reuses the existing blob. Because a tree can only contain entries, an empty directory is literally unrepresentable — hence the <code>.gitkeep</code> convention.</p>\n      <p><strong>What you can do at this point:</strong> <code>git init</code> to create the store, and record a snapshot. Nothing else.</p>\n      <p><strong>What's still broken:</strong> you have a pile of snapshots with no relationship between them — no notion of \"before\" and \"after,\" so no history, just an unordered collection.</p>"
    },
    {
      "anchor": "node-2-link-each-snapshot-to-the-one-it-came-from",
      "title": "Node 2 — Link each snapshot to the one it came from",
      "content": "<p><strong>The limitation:</strong> snapshots exist but aren't ordered.</p>\n      <p>The fix is one field: each commit records the identifier of the commit that preceded it — its <strong>parent</strong>. That single pointer turns the pile into a chain.</p>\n      <p>Because a commit's identifier is a hash of its full content, and its content includes its parent's identifier, and that parent's identifier was a hash including its parent — the identifier of the most recent commit transitively depends on the entire history behind it. Change anything anywhere in the past and every identifier after it changes. This is the actual mechanism behind Git's tamper-evidence.</p>\n      <p>A commit with no parent is a <strong>root commit</strong> — the first one in a repository.</p>\n      <p><strong>What you can do:</strong> <code>git log</code>, which walks parent pointers backward and prints each commit. <code>git show &lt;commit&gt;</code> to inspect any single one.</p>\n      <p><strong>What's still broken:</strong> a snapshot is all-or-nothing, but your working state is never all one thing.</p>"
    },
    {
      "anchor": "node-3-choose-what-goes-into-the-next-snapshot",
      "title": "Node 3 — Choose what goes into the next snapshot",
      "content": "<p><strong>The limitation:</strong> a snapshot captures everything currently in the folder, but the folder always mixes finished work, half-finished work, and junk.</p>\n      <p>Git inserts a middle layer between your folder and the snapshot: the <strong>index</strong>, also called the staging area. It's a real file — <code>.git/index</code> — holding a list of paths and the identifier of the blob that will be recorded for each.</p>\n      <p>So there are three states of your project at any moment: the <strong>working tree</strong> (files on disk), the <strong>index</strong> (what the next snapshot will contain), and <strong>HEAD</strong> (what the last snapshot did contain). Almost every Git command is a movement between them.</p>\n      <p><code>git add</code> hashes a file and points the index at it. <code>git commit</code> turns the index into trees and writes a commit. <code>git status</code> is two comparisons: index-vs-HEAD gives \"staged,\" working-tree-vs-index gives \"unstaged,\" and anything on disk the index never heard of is \"untracked.\" <code>git diff</code> has three forms for the same reason — bare, <code>--staged</code>, and <code>HEAD</code>.</p>\n      <p><code>git add -p</code> stages hunks instead of whole files, which is what makes one-logical-change-per-commit realistic. <code>git commit -a</code> skips the index for tracked files, which is convenient and the single habit most likely to produce incoherent history.</p>\n      <p>The index also caches each file's size and mtime from when it was last hashed — which is why <code>git status</code> doesn't re-hash everything on a large project.</p>\n      <p><strong>.gitignore</strong> handles files that should never be tracked, with negation, anchoring, and directory-only matching. The rule that trips everyone: ignore rules only apply to files Git isn't already tracking — a tracked file stays tracked until you <code>git rm --cached</code> it.</p>\n      <p><strong>What's still broken:</strong> you can only ever be in one place in history, so you still can't work on two things at once.</p>"
    },
    {
      "anchor": "node-4-name-positions-in-history-so-you-can-be-in-more-than-",
      "title": "Node 4 — Name positions in history so you can be in more than one",
      "content": "<p><strong>The limitation:</strong> one line of work at a time; no way to mark a position.</p>\n      <p>A <strong>ref</strong> is a file inside <code>.git/refs/</code> whose entire contents is a commit identifier. A <strong>branch</strong> is therefore a movable pointer to a commit — creating one costs one file write, which is why branching is cheap. Committing while on a branch writes the new commit, then overwrites the branch's file with the new identifier.</p>\n      <p><strong>HEAD</strong> is how Git knows which branch you're on — normally a file containing <code>ref: refs/heads/main</code>, a pointer to a pointer.</p>\n      <p>A commit does not know what branch it's on — \"commits on a branch\" means commits reachable by walking parent links from that branch's pointer. <strong>Detached HEAD</strong> is when HEAD points directly at a commit instead of at a branch — a normal state Git uses deliberately.</p>\n      <p>Revision syntax: <code>HEAD~3</code> walks back three first-parents; <code>HEAD^2</code> means the second parent of a merge; <code>main@{1}</code> means one reflog move back. Range syntax: <code>main..feature</code> means reachable from feature but not main; <code>main...feature</code> is the symmetric difference.</p>\n      <p>Tags mark fixed points — lightweight tags are plain pointers, annotated tags are a real object with tagger/date/message/signature. Tags aren't pushed unless asked.</p>\n      <p><strong>What's still broken:</strong> you can split into two lines of work. You cannot put them back together.</p>"
    },
    {
      "anchor": "node-5-combine-two-lines-of-history",
      "title": "Node 5 — Combine two lines of history",
      "content": "<p><strong>The limitation:</strong> parallel branches diverge and there's no mechanical way to recombine them.</p>\n      <p>Git first finds the <strong>merge base</strong> — the most recent commit reachable from both branches. If the base is your current commit, it's a <strong>fast-forward</strong>: Git just slides your pointer, no new commit. Otherwise it's a <strong>three-way merge</strong>: compare base-to-ours and base-to-theirs per file; one side changed → take it; both changed identically → take once; both changed differently in the same region → conflict.</p>\n      <p>A conflict is not an error — it means two people edited overlapping lines since the base. Git marks it with <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code> / <code>=======</code> / <code>&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code>. Setting <code>merge.conflictStyle=zdiff3</code> also shows the base's version, converting most guesswork conflicts into obvious ones.</p>\n      <p>A successful non-fast-forward merge produces a <strong>merge commit</strong> — a commit with two parents. This is why real history is a graph, not a line.</p>\n      <p><code>--no-ff</code> forces a merge commit for traceability. Squash merge collapses a branch to one commit with no merge link. <strong>Semantic conflicts</strong> — no textual conflict, but the combined code breaks — are exactly why CI runs on the merge result.</p>\n      <p><strong>What's still broken:</strong> history now records what actually happened, including every false start and \"wip\" commit — honest, but unreadable.</p>"
    },
    {
      "anchor": "node-6-reshape-history-before-anyone-else-sees-it",
      "title": "Node 6 — Reshape history before anyone else sees it",
      "content": "<p><strong>The limitation:</strong> the history you produce while working isn't the history you'd want read.</p>\n      <p>Foundational fact: objects are immutable. Every operation here creates <em>new</em> commits and moves a pointer; originals stay in the store, unreferenced.</p>\n      <p><code>git commit --amend</code> replaces the tip commit with a new one. <code>git cherry-pick</code> takes one commit's change and reapplies it elsewhere — internally a three-way merge whose base is the picked commit's parent. <code>git rebase</code> is cherry-pick in a loop: replay every commit unique to your branch onto a new base, then move the pointer to the end.</p>\n      <p>Interactive rebase (<code>rebase -i</code>) gives an editable list: <code>pick</code>, <code>reword</code>, <code>edit</code>, <code>squash</code>, <code>fixup</code>, <code>drop</code>, <code>exec</code>. <code>commit --fixup</code> with <code>--autosquash</code> lets you mark corrections as you go and fold them in automatically. <code>--onto</code> transplants a range onto an arbitrary base.</p>\n      <p>The merge-vs-rebase trade-off: merge preserves what actually happened at the cost of readability; rebase produces a clean line at the cost of claiming events that didn't happen in that order, and changing identifiers. That's the entire reason for the rule against rewriting history others have already pulled — their copy still references the old identifiers.</p>\n      <p><strong>What's still broken:</strong> you now have several operations that can discard work, and you're going to run one by mistake.</p>"
    },
    {
      "anchor": "node-7-undo-and-recover-from-having-undone",
      "title": "Node 7 — Undo, and recover from having undone",
      "content": "<p><strong>The limitation:</strong> Node 6's operations (and Node 3's add, Node 4's branch deletion) can destroy work.</p>\n      <p>Recovery is possible because nothing is ever deleted — commits become unreferenced, not gone, and unreferenced objects are only cleaned up eventually.</p>\n      <p><code>git reset</code> as a table: <strong>--soft</strong> moves HEAD only (changes stay staged); <strong>--mixed</strong> (default) moves HEAD and resets the index (changes stay on disk, unstaged); <strong>--hard</strong> moves HEAD, resets the index, and overwrites the working tree (uncommitted work is gone). <code>git restore</code> splits the file-level cases: <code>--staged</code> to unstage, <code>--worktree</code> to discard.</p>\n      <p><code>git revert</code> is different in kind — a new commit applying the inverse of an old one, so nobody's history is invalidated. It's the correct tool once something is shared.</p>\n      <p><code>git stash</code> shelves uncommitted work (stashes are real commits under a hidden ref). The safety net under everything: the <strong>reflog</strong> — every HEAD/branch value change gets appended locally, with a default expiry around 90 days (reachable) / 30 days (unreachable). After a bad reset or rebase, <code>git reflog</code> plus <code>git reset --hard &lt;that&gt;</code> puts you back.</p>\n      <p><code>git clean</code> deletes untracked files irreversibly — always <code>-n</code> first. <code>ORIG_HEAD</code> is set before every dangerous operation specifically so you can get back.</p>\n      <p><strong>What's still broken:</strong> all of this is on one machine — one disk failure ends it, and no one else can contribute.</p>"
    },
    {
      "anchor": "node-8-move-history-between-machines",
      "title": "Node 8 — Move history between machines",
      "content": "<p><strong>The limitation:</strong> the repository lives on one computer.</p>\n      <p>A <strong>remote</strong> is just a name mapped to a URL. Every clone is a complete, independent repository with the full object store — there's no partial copy and no central authority in the protocol itself.</p>\n      <p><code>git fetch</code> downloads what the other side has that you don't and updates your <strong>remote-tracking branches</strong> (<code>origin/main</code>) — it touches nothing else. <code>origin/main</code> is a local snapshot of the remote's state at your last fetch, not live. <code>git pull</code> is fetch plus an integration step (merge by default) — its implicitness is why so many repos have accidental merge commits. <code>git push</code> uploads objects and asks the remote to move a ref; it's refused if the move wouldn't be a fast-forward.</p>\n      <p>That refusal connects back to Node 6: rebasing a pushed branch means your history no longer descends from the remote's, so the push is rejected. <code>--force-with-lease</code> refuses if the remote moved since your last fetch — targeting the real danger (clobbering someone else's push) rather than the harmless case (rewriting your own unshared history).</p>\n      <p>Supporting machinery: upstream tracking, refspecs, <code>fetch --prune</code>, bare repositories, shallow/partial clones, credential helpers.</p>\n      <p><strong>What's still broken:</strong> multiple people can now push to the same place — which means you need agreements about who pushes what, when, and after whose review.</p>"
    },
    {
      "anchor": "node-9-agree-on-how-a-group-uses-the-shared-repository",
      "title": "Node 9 — Agree on how a group uses the shared repository",
      "content": "<p><strong>The limitation:</strong> Git enforces nothing socially. Two people can both push to <code>main</code> at 2pm.</p>\n      <p>Nothing here is a Git feature — these are conventions and platform features layered on top, necessary because Node 8 makes collaboration possible without making it orderly.</p>\n      <p><strong>Trunk-based development</strong> (frequent integration via short-lived branches) is the dominant modern model. <strong>Feature branch + pull request</strong> is what you'll actually describe in interviews. <strong>Git Flow</strong> (heavyweight, permanent develop/release/hotfix branches) has mostly fallen out of favor for continuous delivery. <strong>Fork-and-PR</strong> is the open-source model.</p>\n      <p>The three merge buttons: regular merge preserves the branch's commits plus a two-parent commit; squash collapses to one commit with no merge link; rebase-merge replays with new identifiers and no merge commit. Small PRs reduce conflict risk mechanically — the longer a branch lives, the further the merge base recedes.</p>\n      <p><strong>What's still broken:</strong> conventions that depend on people remembering them get violated on the day someone is tired.</p>"
    },
    {
      "anchor": "node-10-make-the-agreements-automatic",
      "title": "Node 10 — Make the agreements automatic",
      "content": "<p><strong>The limitation:</strong> conventions are only as reliable as the least-careful person on the least-careful day.</p>\n      <p><strong>Hooks</strong> are scripts Git runs at defined moments — <code>pre-commit</code>, <code>commit-msg</code>, <code>pre-push</code> locally, <code>pre-receive</code> on the server. <code>.git/hooks</code> isn't versioned, which is the entire reason Husky/lefthook/<code>core.hooksPath</code> exist — pointing Git at a tracked, shareable directory instead.</p>\n      <p><code>--no-verify</code> bypasses hooks entirely, which tells you what a hook actually is: a convenience, never a control.</p>\n      <p><strong>Conventional Commits</strong> (<code>feat:</code>, <code>fix:</code>, <code>chore:</code>) is machine-readable, enabling automated changelogs and version bumps. CI running on the merge result (not either branch) is what catches semantic conflicts. Commit signing turns authorship from an unverified text claim into something verifiable. Secret-scanning pre-commit hooks stop a credential from entering history at all — far cheaper than the Node 13 cleanup.</p>\n      <p><strong>What's still broken:</strong> you now have a large history authored mostly by other people, and you need to interrogate it.</p>"
    },
    {
      "anchor": "node-11-ask-questions-of-a-history-you-didn-t-write",
      "title": "Node 11 — Ask questions of a history you didn't write",
      "content": "<p><strong>The limitation:</strong> history is big, old, and largely not yours. \"When did this break, and why\" isn't answerable by scrolling.</p>\n      <p><code>git log</code> flags (<code>--oneline</code>, <code>--graph</code>, <code>--stat</code>, <code>-p</code>, <code>--author</code>, <code>--since</code>). <strong>Pickaxe</strong> (<code>-S</code>) finds commits where a string's occurrence count changed — the fastest way to find when a line was introduced; <code>-G</code> is the regex-presence variant.</p>\n      <p><code>git blame</code> gives per-line last-modifier, with <code>-w</code> to skip whitespace and <code>-M</code>/<code>-C</code> to see through moves and copies; <code>.git-blame-ignore-revs</code> skips bulk-reformat commits.</p>\n      <p><code>git bisect</code> binary-searches history between a known-good and known-bad commit, halving the range each step — <code>bisect run</code> automates it fully, and it depends on commits being atomic and individually buildable, which is the practical payoff of Node 3's discipline. <code>git grep</code> searches tracked content at any revision, faster than a plain recursive grep. <code>git range-diff</code> compares two versions of a rewritten branch.</p>\n      <p><strong>What's still broken:</strong> the repository has been accumulating for years and has started to be slow and large.</p>"
    },
    {
      "anchor": "node-12-deal-with-size-and-scale",
      "title": "Node 12 — Deal with size and scale",
      "content": "<p><strong>The limitation:</strong> an append-only store that never deletes eventually gets big.</p>\n      <p><strong>Packfiles</strong> consolidate many loose objects into one file, storing similar objects as deltas against each other — this is the actual source of Git's storage efficiency, a layer beneath the snapshot model, not a contradiction of it. <code>git gc</code> packs objects, packs refs, and prunes unreferenced objects past their expiry window — the operation that finally makes Node 7's \"recoverable but unreferenced\" genuinely gone.</p>\n      <p>A committed binary is permanent — deleting it in a later commit changes nothing about storage, because the old snapshot still references it; only rewriting history removes it. Git LFS stores large binaries outside the repository. Submodules pin another repo at an exact commit (and land in detached HEAD, because a pin is a commit, not a branch). Worktrees check out a second branch into a second directory sharing one object store — often a better answer than stashing.</p>\n      <p><strong>What's still broken:</strong> history is permanent, which is a problem when what's in it shouldn't be.</p>"
    },
    {
      "anchor": "node-13-trust-secrets-and-irreversibility",
      "title": "Node 13 — Trust, secrets, and irreversibility",
      "content": "<p><strong>The limitation:</strong> permanence is a feature until you commit an API key.</p>\n      <p>A pushed secret is compromised the instant it's pushed — not \"at risk,\" compromised. The correct first action is always to rotate the credential, before any cleanup. <code>git filter-repo</code> is the modern tool for rewriting history to remove a file or string (<code>filter-branch</code> is deprecated and dangerously slow). Rewriting can't fix what it can't reach — existing clones and forks retain the old objects; cleanup makes a secret harder to find, not un-leaked.</p>\n      <p>A deployed <code>.git</code> directory exposes the entire source and history — a real and common vulnerability. Tokens embedded in remote URLs end up in <code>.git/config</code> and shell history. Signed tags extend commit signing's integrity guarantee to release points.</p>"
    },
    {
      "anchor": "where-the-chain-gets-misread",
      "title": "Where the chain gets misread",
      "content": "<ul>\n        <li><strong>\"A commit stores the changes.\"</strong> It stores a complete tree. Diffs are computed on demand; storage stays reasonable because identical content is deduplicated and packfiles compress later.</li>\n        <li><strong>\"A branch contains commits.\"</strong> A branch is one pointer. Membership is reachability — deleting a branch deletes nothing, and one commit can be on many branches.</li>\n        <li><strong>\"Rebase edits commits.\"</strong> It creates new commits with the same changes and different parents. Originals remain until garbage collection.</li>\n        <li><strong>\"git pull just downloads.\"</strong> It downloads and integrates, usually by merging — the source of most unintentional merge commits.</li>\n        <li><strong>\".gitignore will untrack this file.\"</strong> Ignore rules only apply to untracked files. A tracked file stays tracked until explicitly removed from the index.</li>\n        <li><strong>\"I deleted the secret in a new commit, so it's gone.\"</strong> The old snapshot still references it, and it was already exposed regardless.</li>\n        <li><strong>\"Force-push is always dangerous.\"</strong> Force-pushing your own unshared branch is routine; the danger is specific — overwriting someone else's work — and <code>--force-with-lease</code> targets exactly that.</li>\n        <li><strong>\"Detached HEAD means something is broken.\"</strong> It means HEAD points at a commit rather than a branch — a state Git enters deliberately.</li>\n        <li><strong>\"Conflicts mean someone made a mistake.\"</strong> They mean two people edited overlapping lines since the merge base — a normal consequence of parallel work.</li>\n        <li><strong>\"Git and GitHub are the same.\"</strong> Pull requests, reviews, issues, Actions, forks-as-a-feature, and permissions are all GitHub. Git has objects, refs, and a transport protocol.</li>\n      </ul>"
    },
    {
      "anchor": "the-chain-in-one-paragraph",
      "title": "The chain in one paragraph",
      "content": "<p>Git starts by storing whole snapshots of your project in a hidden database, naming each stored thing by a hash of its own contents, which gives it deduplication and tamper-detection for free. Linking each snapshot to its predecessor turns the pile into a history; inserting a staging area between your disk and the snapshot lets you choose what each entry contains; and naming positions in that history with tiny files containing commit identifiers gives you branches, which are cheap precisely because nothing is copied. Once two branches exist they must be recombinable, which requires finding their common ancestor and doing a three-way comparison, which is where conflicts come from and why they aren't errors. The history you actually produce while working is messier than the history you want read, so Git lets you replay commits onto new bases to reshape it — safely, because it never edits anything, only writes new objects and moves pointers, which is also why a log of every pointer movement makes almost any disaster recoverable within a known expiry window. Pushing history to another machine turns those pointer moves into a negotiation, and the refusal to accept a non-fast-forward push is what connects reshaping to collaboration. Everything above that layer — branching strategies, reviews, commit conventions, signing, hooks — is social agreement that Git itself does not enforce, automated only because people forget. And the permanence that makes all of it trustworthy is the same permanence that makes a leaked secret unfixable, which is the one place the design's central property becomes a liability.</p>\n\n    </div>"
    }
  ]
}
