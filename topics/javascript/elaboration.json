{
  "sections": [
    {
      "anchor": "nodes-0-1-values-types-coercion",
      "title": "Nodes 0 & 1 \u2014 The problem before JavaScript, and values, types, and coercion",
      "content": "<h3>Where this sits</h3>\n<p>This is the base of the entire chain. Everything later \u2014 objects, closures, <code>this</code>, promises, the event loop \u2014 is built on the value model described here, and nearly every \"JavaScript is weird\" complaint you will ever hear traces back to a decision made in this node. Nothing depends on anything earlier, because there is nothing earlier.</p>\n<p><em>Nodes 0 and 1 are elaborated together \u2014 Node 0 is purely the setup that makes Node 1 make sense.</em></p>\n\n<h3>Every term, from absolute zero</h3>\n<p><strong>Server</strong> \u2014 a computer, owned by someone else, permanently connected to the internet, whose job is to hold files and hand them out when asked.</p>\n<p><strong>Client</strong> \u2014 the computer doing the asking. Your laptop. In web terminology the client is usually the <em>browser</em> specifically.</p>\n<p><strong>Browser</strong> \u2014 the program on your machine that asks servers for pages and draws them on screen. A browser is not a passive viewer; it is a large, complicated program that reads a document and decides how to paint it.</p>\n<p><strong>HTML</strong> \u2014 HyperText Markup Language. A text format for describing the <em>structure</em> of a document. HTML describes what things are, not what happens when you click them.</p>\n<p><strong>Round trip</strong> \u2014 one complete cycle of your computer sending a request to a server and receiving the reply. The important property is that it takes <em>time you cannot avoid</em> \u2014 the request has to physically travel to the server and the reply has to travel back.</p>\n<p><strong>Latency</strong> \u2014 the delay of that travel, measured in milliseconds. Distinct from <em>bandwidth</em>, which is how much data can flow per second.</p>\n<p><strong>Modem</strong> \u2014 the device that connected home computers to the internet over telephone lines in the 1990s, at speeds around 28,800 bits per second \u2014 roughly 3.5 kilobytes per second.</p>\n<p><strong>Form validation</strong> \u2014 checking that what someone typed into a form is acceptable before doing anything with it.</p>\n<p><strong>Interpreter</strong> \u2014 a program that reads source code and executes it directly, line by line. Contrast with a <strong>compiler</strong>, which translates all the source first into a different form that gets run later as a separate step. A language delivered over the network to run instantly cannot afford a build step, which pushed JavaScript toward interpretation.</p>\n<p><strong>Netscape</strong> \u2014 the company that made Netscape Navigator, the dominant web browser of the mid-1990s.</p>\n<p><strong>Brendan Eich</strong> \u2014 the programmer Netscape hired in 1995 to add a scripting language to their browser. He wrote the first version in about ten days in May 1995.</p>\n<p><strong>Java</strong> \u2014 a completely separate programming language, released by Sun Microsystems in 1995, extremely fashionable at that moment. It has essentially nothing to do with JavaScript. The name was borrowed for marketing.</p>\n<p><strong>Backward compatibility</strong> \u2014 the property that new versions continue to work with everything built for old versions. In JavaScript this is close to absolute, and it has a name in the standards community: <strong>\"don't break the web.\"</strong></p>\n<p><strong>Value</strong> \u2014 a single piece of data. The number 5. The text \"hello\". True.</p>\n<p><strong>Variable</strong> \u2014 a name that refers to a value. More precisely a <strong>binding</strong>: a connection between a name and a value.</p>\n<p><strong>Type</strong> \u2014 the <em>kind</em> of a value, which determines what operations make sense on it.</p>\n<p><strong>Dynamically typed</strong> \u2014 a language where a variable's type is not fixed in advance and is only known when the program runs. Contrast with <strong>statically typed</strong>, where types are declared or inferred and checked before the program runs.</p>\n<p><strong>Primitive</strong> \u2014 a value that is a single indivisible thing with no internal parts you can reach into and change. There are seven primitive types. The alternative is an <strong>object</strong>, a collection of named parts \u2014 Node 2's subject.</p>\n<p><strong>Bit</strong> \u2014 a single 0 or 1. The smallest possible piece of information.</p>\n<p><strong>Binary</strong> \u2014 counting using only 0 and 1, where each position is worth twice the one to its right.</p>\n<p><strong>Floating point</strong> \u2014 a way of storing numbers that can represent very large and very small values, and fractions, by storing significant digits alongside a separate exponent saying where the point sits. The point \"floats\" rather than being fixed.</p>\n<p><strong>IEEE 754</strong> \u2014 the international standard, published in 1985, defining exactly how floating-point numbers are stored and how arithmetic on them behaves. Nearly every language implements it, which is why floating-point oddities are shared across languages rather than being JavaScript-specific.</p>\n<p><strong>Integer</strong> \u2014 a whole number with no fractional part. JavaScript has no separate integer type for ordinary arithmetic.</p>\n<p><strong>Unicode</strong> \u2014 the standard assigning a unique number, a <strong>code point</strong>, to every character in every writing system, plus emoji and symbols. About 1.1 million possible code points, roughly 150,000 currently assigned.</p>\n<p><strong>Code point</strong> \u2014 one Unicode number identifying one character, written like <code>U+1F44B</code>, where the digits are hexadecimal (base 16).</p>\n<p><strong>Code unit</strong> \u2014 the fixed-size chunk actually used to store text in a particular encoding. In <strong>UTF-16</strong>, which JavaScript strings use, each code unit is 16 bits. Small code points fit in one; large ones need two.</p>\n<p><strong>Immutable</strong> \u2014 cannot be changed after creation. Any operation that appears to change it produces a new value instead.</p>\n<p><strong>Coercion</strong> \u2014 automatic conversion of a value from one type to another, performed silently when an operation is given a type it didn't expect.</p>\n<p><strong>Truthy / falsy</strong> \u2014 how a value behaves where a true-or-false answer is required. A <strong>falsy</strong> value behaves as false; everything else is <strong>truthy</strong>. There are exactly eight falsy values.</p>\n<p><strong><code>typeof</code></strong> \u2014 a built-in operator returning text naming a value's type: <code>\"number\"</code>, <code>\"string\"</code>, <code>\"boolean\"</code>, <code>\"undefined\"</code>, <code>\"object\"</code>, <code>\"function\"</code>, <code>\"symbol\"</code>, <code>\"bigint\"</code>.</p>\n\n<h3>The history \u2014 why this exists at all</h3>\n<p>In 1994, a web page was a document and nothing more. The server sent HTML, the browser drew it, and the interaction ended. If a page contained a form, the only thing the browser could do with what you typed was package it up and send it back to the server. The server then examined it, and if the email address was missing an <code>@</code>, wrote an entirely new HTML page whose content was \"invalid email address,\" and sent that back.</p>\n<p>Over a 28,800-bit-per-second modem, that round trip took a noticeable, irritating second or more. And it happened <em>per problem</em>. If a form had four fields wrong, you discovered them one at a time, in four separate round trips, because the server would typically stop at the first failure. Filling in a form was an exercise in patience.</p>\n<p>The obvious fix \u2014 let some code run inside the browser so that \"does this contain an <code>@</code>\" gets answered instantly \u2014 was obvious to everyone. But the <em>constraints</em> on such a language were unusual and they are what produced the language we have.</p>\n<p>The code arrives as plain text, embedded in a page, from a server the user has never heard of. That means four things at once. It must begin executing essentially immediately, so there can be no compilation step the user waits through \u2014 which pushed hard toward an interpreter. It must be incapable of harming the machine it lands on, because it came from a stranger, so it gets no access to the filesystem and no ability to act outside the page. It must not be able to freeze the browser, because the browser has a page to draw. And \u2014 the constraint that outlasted all the others \u2014 it must never break existing pages, because there is no mechanism to go back and update the millions of pages already published by people who have moved on.</p>\n<p>Netscape hired Brendan Eich in April 1995 and gave him roughly ten days in May to produce a working prototype. Ten days is not long enough to design a language. It is barely long enough to implement one you have already designed. The consequences are visible throughout: the language shipped with rough edges that were noticed almost immediately and could never be removed, because by the time anyone agreed they were mistakes, pages depended on them.</p>\n<p>The name is pure marketing. The language was called <strong>Mocha</strong> internally, then <strong>LiveScript</strong> when it shipped in a Netscape beta in September 1995, and then renamed <strong>JavaScript</strong> in December 1995 as part of a partnership announcement with Sun Microsystems, whose Java language was the most fashionable thing in computing that year. The two languages are unrelated. This confusion is thirty years old and still generates interview questions.</p>\n<p>One design decision from that period matters more than any other for this node. Eich was building a language for people writing small scripts in web pages \u2014 designers, hobbyists, people who were not professional programmers. A language that stopped and complained \"you gave me text where I expected a number\" would frustrate exactly that audience. So the language was built to <em>keep going</em>: when given a type it didn't expect, it would convert the value and carry on rather than failing. That is coercion, and it is the single most consequential decision in JavaScript's design. It made the language forgiving for beginners writing five-line scripts, and treacherous for professionals writing fifty-thousand-line applications \u2014 which is what everyone ended up doing.</p>\n<p>The language was submitted to a standards body called <strong>Ecma International</strong> in 1996 and the first standard, <strong>ECMAScript</strong>, was published in June 1997. The name \"JavaScript\" was trademarked by Sun, so the standard needed a different one; that is why the specification and the language have different names. From 1997 onward, changing existing behaviour required convincing a committee that it wouldn't break the web \u2014 and it almost always would.</p>\n\n<h3>The mechanism \u2014 traced</h3>\n\n<h4>How a number is actually stored</h4>\n<p>Every ordinary number in JavaScript occupies 64 bits, arranged according to IEEE 754's \"double precision\" format. Those 64 bits are divided into three fields:</p>\n<ul>\n<li><strong>1 bit</strong> for the sign: 0 means positive, 1 means negative.</li>\n<li><strong>11 bits</strong> for the exponent: roughly which power of two the number sits near.</li>\n<li><strong>52 bits</strong> for the fraction (also called the mantissa or significand): the actual significant digits.</li>\n</ul>\n<p>The value is reconstructed as roughly <code>sign &times; 1.fraction &times; 2^(exponent &minus; 1023)</code>. The 1023 is a fixed offset that lets the 11-bit exponent field represent both positive and negative exponents using only non-negative bit patterns.</p>\n<p>Notice the leading <code>1.</code> in that formula. Because any non-zero binary number can be written with exactly one <code>1</code> before the point, that leading digit is always 1 and therefore doesn't need storing. It is implied. So although only 52 bits are physically stored for the fraction, you get <strong>53 bits of precision</strong>. That number, 53, governs everything else in this section.</p>\n\n<h4>Why 0.1 + 0.2 isn't 0.3</h4>\n<p>Write one-third in decimal and you get 0.3333\u2026 forever. It is not that decimal is broken; one-third simply has no exact finite decimal representation. Which fractions <em>do</em> terminate depends on the base you are writing in.</p>\n<p>In base ten, a fraction terminates if its denominator's only prime factors are 2 and 5. In base two, only 2. One-tenth is 1/10, and 10 = 2 &times; 5. That 5 is fatal: <strong>one-tenth has no exact binary representation.</strong> In binary it is <code>0.0001100110011001100\u2026</code>, repeating forever, exactly the way one-third repeats in decimal.</p>\n<p>So when you write <code>0.1</code>, the engine stores the closest value it can fit in 53 bits of precision. That value is not 0.1. It is:</p>\n<pre><code>0.1000000000000000055511151231257827021181583404541015625</code></pre>\n<p>Similarly <code>0.2</code> is stored as a value slightly above two-tenths. Add those two slightly-wrong values and the errors compound rather than cancel, producing a result whose nearest 53-bit representation prints as <code>0.30000000000000004</code>.</p>\n<p>This is not a JavaScript bug. Java, C, C++, Python, C#, Ruby and Go all produce the same result, because they all use IEEE 754 doubles. What is JavaScript-specific is having <em>no alternative</em> \u2014 no integer type, no built-in decimal type \u2014 so you meet floating point even when doing arithmetic on whole numbers.</p>\n<p>The practical consequence: never compare floating-point results with <code>===</code>. Compare the absolute difference against a small tolerance instead. And never store money as a JavaScript number. Store it as a whole number of the smallest unit \u2014 paise, cents \u2014 which stays exact inside the safe integer range, and format for display at the last possible moment.</p>\n\n<h4>The safe integer limit</h4>\n<p>Whole numbers are stored in that same 53-bit precision. Any integer up to 2^53 fits exactly. Above that, there are more integers than 53 bits can distinguish, so the engine stores the nearest representable one \u2014 and above 2^53 the representable values are spaced two apart.</p>\n<p><code>Number.MAX_SAFE_INTEGER</code> is <strong>9,007,199,254,740,991</strong>, exactly 2^53 &minus; 1. It is called <em>safe</em> because it is the largest integer <code>n</code> for which both <code>n</code> and <code>n + 1</code> are exactly representable. Past it:</p>\n<pre><code>9007199254740992 === 9007199254740993  // true</code></pre>\n<p>Two different integers, indistinguishable, silently. Nothing throws.</p>\n<p>This is not academic. Twitter's post IDs exceeded it in 2011 and every JavaScript client that parsed them as numbers silently corrupted them. Database primary keys of type <code>bigint</code>, timestamps in nanoseconds, and financial values in minor units all cross it routinely.</p>\n<p><code>bigint</code>, added in ES2020, is the fix: a separate primitive type for arbitrary-precision integers, written with an <code>n</code> suffix (<code>9007199254740993n</code>). It is exact at any size, but deliberately <em>not</em> interchangeable with <code>number</code> \u2014 mixing them in arithmetic throws a <code>TypeError</code>, precisely so you cannot accidentally launder an exact value through an inexact one.</p>\n<p>Three special values also live in the number type. <code>Infinity</code> and <code>-Infinity</code> come from overflow or from dividing by zero \u2014 <code>1/0</code> is <code>Infinity</code>, not an error. And <code>NaN</code>, \"not a number\", is the result of arithmetic with no meaningful answer: <code>0/0</code>, <code>Math.sqrt(-1)</code>, <code>Number(\"hello\")</code>.</p>\n<p><code>NaN</code> has a property mandated by IEEE 754 that surprises everyone: <strong>it is not equal to itself.</strong> <code>NaN === NaN</code> is <code>false</code>. The reasoning is that NaN represents \"the result of some failed computation,\" and two separate failures are not meaningfully \"the same value.\" So you cannot test for NaN with <code>===</code>. Use <code>Number.isNaN(x)</code>. Do <em>not</em> use the older global <code>isNaN(x)</code> \u2014 it coerces its argument first, so <code>isNaN(\"hello\")</code> is <code>true</code> even though <code>\"hello\"</code> is a string, not NaN.</p>\n<p>There is also <code>-0</code>, negative zero, which exists because the sign bit is stored separately from the magnitude. <code>-0 === 0</code> is <code>true</code>, but <code>1 / -0</code> is <code>-Infinity</code> while <code>1 / 0</code> is <code>Infinity</code>. <code>Object.is(-0, 0)</code> correctly reports <code>false</code>.</p>\n\n<h4>How a string is actually stored</h4>\n<p>A JavaScript string is a sequence of 16-bit code units, encoded in UTF-16.</p>\n<p>When JavaScript was designed, Unicode was expected to need at most 16 bits per character \u2014 65,536 possible characters seemed like plenty for every writing system. Several languages of that era, JavaScript and Java included, baked that assumption in. It turned out to be wrong. Unicode grew past 65,536, and UTF-16 was extended with a mechanism to encode larger code points using <em>two</em> 16-bit units, called a <strong>surrogate pair</strong>.</p>\n<p>A range of code points, <code>U+D800</code> through <code>U+DFFF</code>, was permanently reserved for this and assigned no characters of their own. A large code point is split across one \"high surrogate\" from <code>U+D800</code>\u2013<code>U+DBFF</code> and one \"low surrogate\" from <code>U+DC00</code>\u2013<code>U+DFFF</code>.</p>\n<p>The waving-hand emoji is code point <code>U+1F44B</code>. That is far above 65,535, so it is stored as the pair <code>0xD83D 0xDC4B</code>. And since <code>.length</code> counts <strong>code units</strong>, not characters:</p>\n<pre><code>\"\ud83d\udc4b\".length        // 2\n\"\ud83d\udc4b\"[0]            // an unpaired surrogate \u2014 a broken half-character\n[...\"\ud83d\udc4b\"].length   // 1 \u2014 spread uses the iteration protocol, which is code-point aware</code></pre>\n<p>This is why naive string truncation splits emoji in half and produces a replacement glyph. <code>for...of</code>, spread, and <code>Array.from</code> all iterate by code point and behave correctly; indexing and <code>.length</code> do not.</p>\n<p>Strings are immutable. <code>s[0] = \"x\"</code> fails silently \u2014 it does nothing at all, and in strict mode it throws. Every string method returns a new string.</p>\n\n<h4>What coercion actually does</h4>\n<p>Coercion has two directions, and keeping them apart is most of the battle.</p>\n<p><strong>To boolean</strong>, when a value is used as a condition. Exactly eight values are falsy: <code>false</code>, <code>0</code>, <code>-0</code>, <code>0n</code>, <code>\"\"</code>, <code>null</code>, <code>undefined</code>, and <code>NaN</code>. Everything else is truthy \u2014 including <code>\"0\"</code>, the string <code>\"false\"</code>, the empty array <code>[]</code>, and the empty object <code>{}</code>. Arrays and objects are truthy because they are objects, and <em>all</em> objects are truthy regardless of contents. (There is exactly one bizarre exception, <code>document.all</code> in browsers, deliberately made falsy to keep 1990s pages working \u2014 a perfect illustration of \"don't break the web.\")</p>\n<p><strong>Between types, for comparison and arithmetic.</strong> <code>==</code> runs an algorithm the specification calls Abstract Equality Comparison:</p>\n<ol>\n<li>If both operands are the same type, compare them directly (identical to <code>===</code>).</li>\n<li>If one is <code>null</code> and the other <code>undefined</code>, return <code>true</code>. This special case is why <code>x == null</code> is a legitimate idiom \u2014 it catches both, and nothing else.</li>\n<li>If one is a number and the other a string, convert the string to a number.</li>\n<li>If either is a boolean, convert it to a number first \u2014 <code>true</code> becomes <code>1</code>, <code>false</code> becomes <code>0</code>.</li>\n<li>If one is an object and the other a primitive, convert the object to a primitive by calling its <code>valueOf()</code>, and if that does not return a primitive, its <code>toString()</code>.</li>\n</ol>\n<p>Now trace the notorious <code>[] == false</code>, which is <code>true</code> even though <code>[]</code> is truthy:</p>\n<ul>\n<li><code>false</code> is a boolean, so by step 4 it becomes the number <code>0</code>. Now: <code>[] == 0</code>.</li>\n<li><code>[]</code> is an object and <code>0</code> is a primitive, so by step 5 the array is converted. <code>[].valueOf()</code> returns the array itself, not a primitive, so <code>[].toString()</code> is used, producing the empty string <code>\"\"</code>. Now: <code>\"\" == 0</code>.</li>\n<li>String versus number, so by step 3 <code>\"\"</code> converts to the number <code>0</code>. Now: <code>0 == 0</code>, which is <code>true</code>.</li>\n</ul>\n<p>There is no contradiction with <code>[]</code> being truthy. Truthiness asks \"how does this behave as a condition\"; <code>==</code> runs a conversion algorithm between two operands. Different questions, different rules \u2014 conflating them is what makes this feel arbitrary.</p>\n<p><code>===</code>, strict equality, skips all of it: different types means <code>false</code>, full stop. Use it always, with <code>x == null</code> as the one defensible exception.</p>\n<p>Finally, <code>typeof null</code> returns <code>\"object\"</code>. In the original 1995 implementation, values were stored as a small tag identifying the type plus the data itself; the tag for objects was zero, and <code>null</code> was represented as all zeros \u2014 so its tag read as \"object\". It was recognised as wrong almost immediately. A fix was formally proposed and rejected, because too much existing code checks <code>typeof x === \"object\"</code> in ways that would break. It is now permanent.</p>\n\n<h3>In your project</h3>\n<p>Stage 1 of <code>shortlist</code> walks you into most of this without asking. Job boards return payloads where some records have a <code>location</code> and some do not, so you will print <code>undefined</code> into a string and see it rendered as the literal text <code>\"undefined\"</code> \u2014 coercion to string, working exactly as designed and giving you nonsense. You will reach for <code>job.salary || \"Not listed\"</code> and it will be correct until a genuine <code>0</code> or <code>\"\"</code> shows up and gets overridden, which is the moment <code>??</code> stops being a syntax variant and becomes the tool that means what you meant.</p>\n<p>Stage 5 is where the number model bites hardest. Once you are comparing and averaging salaries across currencies, doing it in floating point produces averages wrong in the last few digits and comparisons that fail at boundaries. The spec deliberately asks you to get this wrong once with floats and look at the output, then store everything as an integer number of minor units plus a currency code. Salary parsing also means reading <code>\"$120k\"</code> and <code>\"\u20b918,00,000\"</code> as numbers, which is <code>parseInt</code> versus <code>Number</code> territory: <code>parseInt(\"120k\")</code> gives <code>120</code> by parsing leading digits and stopping, while <code>Number(\"120k\")</code> gives <code>NaN</code> because it demands the entire string be numeric.</p>\n<p>And the fingerprinting for deduplication is string work end to end \u2014 case folding, punctuation stripping, comparing <code>\"Senior Engineer\"</code> against <code>\"Sr. Engineer\"</code>. If any company name in your dataset contains a character outside the basic range, <code>.length</code>-based truncation will split it.</p>\n\n<h3>In qbank</h3>\n<p>There is one honest connection worth knowing, and it is the kind of thing that impresses in an interview because almost nobody can explain it.</p>\n<p>qbank uses Postgres through Drizzle. If any column in that schema is a Postgres <code>bigint</code> (a 64-bit integer, which can hold values far beyond 9,007,199,254,740,991), the underlying Node Postgres driver returns those values as <strong>strings</strong>, not numbers \u2014 and people routinely file this as a bug. It is not. The driver cannot return them as JavaScript numbers without risking silent corruption of any value above <code>MAX_SAFE_INTEGER</code>, so it hands back a string and lets you decide. That decision exists purely because of the 53-bit precision limit traced above. Worth checking your actual schema to see whether you have any <code>bigint</code> columns, so you can speak about it concretely rather than in the abstract.</p>\n<p>The other place this touches qbank is JSON coming back from the Anthropic API. Any number in a JSON payload becomes a JavaScript number when parsed, with the same precision limit \u2014 which is one reason IDs in APIs are so often strings rather than numbers.</p>\n\n<h3>What an interviewer does with this</h3>\n<p><strong>\"Why does <code>0.1 + 0.2 !== 0.3</code>?\"</strong> A weak answer is \"floating point rounding errors\" and stops. A strong answer explains that one-tenth has no exact binary representation because 10 has a prime factor of 5, that the stored value is therefore slightly off, that the errors compound when added, and \u2014 the part that separates candidates \u2014 that this is identical in Java, Python and C, so it is not a JavaScript quirk. Then add what you <em>do</em> about it: compare with a tolerance, store money as integer minor units.</p>\n<p><strong>\"What's the difference between <code>null</code> and <code>undefined</code>?\"</strong> A weak answer says \"they're both empty.\" A strong one is precise about who assigns them: <code>undefined</code> is what the language supplies when nothing was provided; <code>null</code> is what a programmer assigns deliberately to mean \"empty on purpose.\" Then mention that <code>typeof null</code> is <code>\"object\"</code>, a 1995 implementation artefact kept for compatibility, and that <code>x == null</code> is the idiomatic check for both.</p>\n<p><strong>\"What are the falsy values?\"</strong> Recite all eight: <code>false</code>, <code>0</code>, <code>-0</code>, <code>0n</code>, <code>\"\"</code>, <code>null</code>, <code>undefined</code>, <code>NaN</code>. The follow-up is usually \"is <code>[]</code> falsy?\" No \u2014 all objects are truthy. If they push on <code>[] == false</code> being <code>true</code>, walk the coercion steps.</p>\n<p><strong>\"<code>==</code> or <code>===</code>?\"</strong> Always <code>===</code>, because <code>==</code> runs a conversion algorithm whose results are hard to predict \u2014 except <code>x == null</code>, a well-understood idiom for catching both <code>null</code> and <code>undefined</code>. Being able to name the exception signals you know the rule rather than reciting advice.</p>\n<p><strong>\"Why does JavaScript have <code>bigint</code>?\"</strong> Because all ordinary numbers are 64-bit doubles with 53 bits of precision, integers stay exact only up to 2^53 &minus; 1, and real-world values \u2014 database IDs, nanosecond timestamps, financial amounts in minor units \u2014 exceed that. Mentioning that <code>bigint</code> deliberately refuses to mix with <code>number</code> in arithmetic is the detail that shows you have actually used it.</p>\n"
    },
    {
      "anchor": "node-2-objects-and-references",
      "title": "Node 2 \u2014 Objects, and the reference model",
      "content": "<h3>Where this sits</h3>\n<p>This node divides the language in half. Node 1 covered the seven primitive types; everything that is not one of those is an object, and objects follow completely different rules for copying, comparison, and assignment. Nodes 4 (closures), 6 (prototypes), 7 (collections), and 15 (memory leaks) all depend on understanding what a reference actually is, and the single most common category of real bug in working JavaScript code lives here.</p>\n\n<h3>Every term, from absolute zero</h3>\n<p><strong>Object</strong> \u2014 a value that is a <em>collection of named parts</em>. Where a primitive is one indivisible thing, an object holds many things, each reachable by a name.</p>\n<p><strong>Property</strong> \u2014 one named part of an object. A property has a <strong>key</strong> (its name) and a <strong>value</strong> (what it holds).</p>\n<p><strong>Key</strong> \u2014 the name identifying a property. Keys can only be <strong>strings</strong> or <strong>symbols</strong>. Anything else used as a key is silently converted to a string first.</p>\n<p><strong>Object literal</strong> \u2014 the syntax for writing an object directly in source code, using curly braces: <code>{ a: 1, b: 2 }</code>.</p>\n<p><strong>Dot notation</strong> \u2014 accessing a property by writing the key after a dot: <code>user.name</code>. Requires knowing the key when you write the code.</p>\n<p><strong>Bracket notation</strong> \u2014 accessing a property by writing an expression in square brackets: <code>user[\"name\"]</code> or <code>user[keyVariable]</code>. Required whenever the key is computed or held in a variable.</p>\n<p><strong>Memory</strong> \u2014 the working storage a running program uses to hold values. You never manage it directly in JavaScript, but you need the word to talk about what a reference points at.</p>\n<p><strong>Reference</strong> \u2014 a value that identifies <em>where</em> something is stored rather than being the thing itself. If a primitive is a written number, a reference is a note saying \"the number is in the third drawer.\" Copying the note gives you two notes pointing at one drawer, not two drawers.</p>\n<p><strong>Pass by value</strong> \u2014 the receiver gets an independent copy; changing it cannot affect the original. This is how all seven primitive types behave.</p>\n<p><strong>Pass by reference</strong> \u2014 the receiver gets a reference to the <em>same</em> underlying thing, so changes are visible everywhere. This is how all objects behave.</p>\n<p><strong>Mutation</strong> \u2014 changing an object in place while it remains the same object. Distinct from <strong>reassignment</strong>, which points a variable at an entirely different value.</p>\n<p><strong>Identity</strong> \u2014 whether two references point to the same underlying object. Distinct from <strong>structural equality</strong>, which asks whether two objects have the same contents. JavaScript's <code>===</code> tests identity for objects, never structure.</p>\n<p><strong>Shallow copy</strong> \u2014 a new object whose top-level properties are copied. Any property whose value was itself an object is copied <em>as a reference</em>, so both objects still share that nested object.</p>\n<p><strong>Deep copy</strong> \u2014 a new object where every level of nesting has been duplicated independently, sharing nothing with the original.</p>\n<p><strong>Circular reference</strong> \u2014 an object that, following its properties, eventually leads back to itself. <code>a.self = a</code> is the simplest case. Circular references break naive copying and break <code>JSON.stringify</code>.</p>\n<p><strong>Nullish</strong> \u2014 meaning <code>null</code> or <code>undefined</code>, and specifically <em>not</em> the other falsy values. The word exists because operators added in 2020 needed a name for exactly that pair.</p>\n<p><strong>Short-circuit</strong> \u2014 an operator that stops evaluating as soon as the answer is determined. In <code>a || b</code>, if <code>a</code> is truthy the answer is <code>a</code> and <code>b</code> is never evaluated.</p>\n<p><strong>Enumerable</strong> \u2014 a property that shows up when you list an object's keys. Properties you write normally are enumerable; some built-in ones deliberately are not. This is the shallow version of Node 16's material.</p>\n\n<h3>The history \u2014 why this exists at all</h3>\n<p>Node 1 gave you individual values. That is enough to write a calculator and nothing else. The moment you want to represent a person, an order, a job posting, or an HTTP request, you need to hold several values together and be able to say which is which.</p>\n<p>The two classic answers in programming languages are the <strong>record</strong> \u2014 a fixed set of named fields, declared in advance, checked by the compiler \u2014 and the <strong>dictionary</strong> (or hash map, or associative array) \u2014 an open-ended collection of key-value pairs you can add to and remove from at runtime. Records are what C <code>struct</code>s and Java classes give you: rigid, fast, checked. Dictionaries are what Python's <code>dict</code> and Perl's hashes give you: flexible, dynamic, unchecked.</p>\n<p>JavaScript, being dynamically typed and designed in ten days for people writing small scripts, went entirely with the dictionary. There is no declaration step, no fixed field list, no check. An object is a bag of key-value pairs, and you may add a property at any moment, delete one, or ask for one that was never there \u2014 the last of which returns <code>undefined</code> rather than failing. That decision is why <code>user.emial</code> is a legal expression that quietly evaluates to <code>undefined</code> instead of an error, which is the exact failure mode Node 18 eventually names as the language's core limitation.</p>\n<p>The consequential decision, though, is not the dictionary shape \u2014 it is <strong>how objects are handled when you move them around</strong>. Here JavaScript followed a convention nearly every language of its lineage shares: primitives are copied, objects are shared. The reason is practical. An object can be arbitrarily large. If passing one to a function meant duplicating the whole thing, every call carrying an object would be expensive, and there would be no way for a function to modify a structure its caller can see. So what actually gets passed is a small reference, and everyone holding a reference is looking at the same object.</p>\n<p>This is genuinely the right engineering trade, and it is also the source of an enormous fraction of real bugs, because the sharing is invisible. Nothing in the syntax distinguishes <code>const b = a</code> where <code>a</code> is a number from <code>const b = a</code> where <code>a</code> is an object, but the first gives you independence and the second gives you an alias.</p>\n<p>One more piece of history matters. The 2015 standard added <strong>spread</strong> syntax (<code>{...obj}</code>) and 2017 finished the object utilities, and together these made a particular style popular: instead of mutating an object, build a new one with the changes applied. This \"treat objects as immutable\" discipline arrived largely from the React ecosystem, where change detection depends on comparing object identity cheaply \u2014 if you mutate an object in place, its identity is unchanged and nothing can tell anything happened. That is why <code>setState({...state, count: 5})</code> looks the way it does.</p>\n\n<h3>The mechanism \u2014 traced</h3>\n\n<h4>What an object actually is</h4>\n<pre><code>const user = { name: \"Devansh\", city: \"Noida\" };</code></pre>\n<p>Three things happened. An object was created in memory, holding two properties. A reference to that object was produced. And the name <code>user</code> was bound to that reference. The variable <code>user</code> does not contain the object \u2014 it contains a reference to it. This distinction is invisible in the syntax and it is the whole node.</p>\n<p>Keys are strings or symbols, and only those:</p>\n<pre><code>const obj = {};\nobj[1] = \"a\";\nobj[\"1\"] = \"b\";\nobj[1];              // \"b\" \u2014 the same property\nObject.keys(obj);    // [\"1\"] \u2014 one key, a string</code></pre>\n<p>The number <code>1</code> was converted to the string <code>\"1\"</code>, so both writes hit one property. This is why <code>Map</code> exists (Node 7) \u2014 it accepts keys of any type without converting them.</p>\n<p>Arrays follow directly. An array is an object whose keys are the string forms of consecutive integers, plus a <code>length</code> the engine maintains:</p>\n<pre><code>const arr = [\"a\", \"b\"];\ntypeof arr;          // \"object\"\nObject.keys(arr);    // [\"0\", \"1\"]\narr[\"0\"];            // \"a\"</code></pre>\n<p><code>typeof</code> cannot distinguish an array from a plain object, which is why <code>Array.isArray()</code> exists. Functions are objects too, with the one carve-out that <code>typeof</code> reports <code>\"function\"</code> \u2014 a special case in the specification, not derivable from the rules. Dates, regular expressions, <code>Map</code>, <code>Set</code>, and <code>Error</code> instances are all objects as well.</p>\n\n<h4>Dot versus bracket</h4>\n<pre><code>user.name          // key written literally in the source\nuser[\"name\"]       // identical result\nconst k = \"name\";\nuser[k]            // the only way to use a key held in a variable\nuser.k             // WRONG \u2014 looks for a property literally named \"k\"</code></pre>\n<p>Bracket notation is also required for keys that are not valid identifiers \u2014 <code>obj[\"first name\"]</code>, <code>obj[\"2fa-enabled\"]</code>.</p>\n\n<h4>The reference model, demonstrated</h4>\n<pre><code>let a = 5;\nlet b = a;\nb = 10;\na;   // 5 \u2014 completely unaffected</code></pre>\n<p><code>b</code> received a copy of the value. There is no connection.</p>\n<pre><code>const a = { count: 5 };\nconst b = a;\nb.count = 10;\na.count;   // 10 \u2014 the SAME object was modified</code></pre>\n<p><code>b</code> received a copy of the <em>reference</em>. Both names point at one object. There is only ever one object here.</p>\n<pre><code>function addTag(job) {\n  job.tags = [\"remote\"];      // mutates the caller's object\n}\nfunction replace(job) {\n  job = { tags: [\"remote\"] }; // rebinds the local parameter only\n}</code></pre>\n<p><code>addTag</code> changes what the caller sees. <code>replace</code> does not \u2014 it pointed the local name at a brand new object, leaving the caller's reference untouched. This is why the precise description is \"pass by value, where the value being passed is a reference.\" Reassigning the parameter does nothing outward; mutating through it does.</p>\n\n<h4>Why const is weaker than it looks</h4>\n<pre><code>const user = { name: \"Devansh\" };\nuser.name = \"changed\";     // fine\nuser.role = \"engineer\";    // fine\nuser = {};                 // TypeError: Assignment to constant variable</code></pre>\n<p><code>const</code> freezes the <strong>binding</strong> \u2014 the connection between the name and the reference. It says nothing about the object. <code>const arr = []; arr.push(1)</code> is legal for exactly this reason.</p>\n<pre><code>const user = Object.freeze({ name: \"Devansh\", address: { city: \"Noida\" } });\nuser.name = \"x\";              // silently ignored (throws in strict mode)\nuser.address.city = \"Delhi\";  // WORKS \u2014 freeze is shallow</code></pre>\n<p><code>Object.freeze</code> prevents adding, removing, or changing the object's <em>own</em> properties. It does not touch nested objects, because those are separate objects reached through a reference.</p>\n\n<h4>Equality is identity</h4>\n<pre><code>const a = { x: 1 };\nconst b = { x: 1 };\nconst c = a;\n\na === b;   // false \u2014 two different objects\na === c;   // true  \u2014 same object</code></pre>\n<p><code>===</code> on objects asks one question: <em>are these the same object?</em> It never inspects contents. There is no built-in deep equality \u2014 your options are a recursive comparison, a library, or (only for simple JSON-safe data) comparing <code>JSON.stringify</code> output, which is fragile because key order matters.</p>\n<p>This identity rule is also <em>why</em> the immutable-update style exists. If you mutate an object in place its identity is unchanged, so code that cached the old reference and compares with <code>===</code> concludes nothing happened. Build a new object instead, and the identity change is the signal.</p>\n\n<h4>Copying, and the shallow/deep boundary</h4>\n<pre><code>const original = { name: \"Devansh\", address: { city: \"Noida\" } };\n\nconst shallow = { ...original };\nshallow.name = \"changed\";\noriginal.name;          // \"Devansh\" \u2014 top level is independent\n\nshallow.address.city = \"Delhi\";\noriginal.address.city;  // \"Delhi\" \u2014 nested object is SHARED</code></pre>\n<p>The spread copied two properties: the string (a primitive \u2014 copied by value) and the reference to the address object (copied as a reference). One new outer object was created; the inner object was never duplicated. <code>Object.assign({}, original)</code> does exactly the same thing.</p>\n<pre><code>const deep = structuredClone(original);\ndeep.address.city = \"Delhi\";\noriginal.address.city;  // \"Noida\" \u2014 untouched</code></pre>\n<p><code>structuredClone</code> is built in, available in Node 17+ and all current browsers. It handles arbitrary nesting, <code>Map</code>, <code>Set</code>, <code>Date</code>, <code>RegExp</code>, typed arrays, and <strong>circular references</strong>, which naive recursive copying loops forever on. What it cannot clone: functions, DOM nodes, and class instances lose their prototype and come back as plain objects. A function throws <code>DataCloneError</code> \u2014 the same error you meet in Stage 6 of <code>shortlist</code>, because workers use the same cloning algorithm.</p>\n<p>The old trick <code>JSON.parse(JSON.stringify(obj))</code> also deep-copies, badly: it destroys <code>Date</code> objects into strings, drops <code>undefined</code> and functions, throws on circular references, and cannot represent <code>Map</code> or <code>Set</code>.</p>\n\n<h4>The utilities you will actually use</h4>\n<pre><code>Object.keys(user)     // [\"name\", \"city\"] \u2014 own enumerable string keys\nObject.values(user)   // [\"Devansh\", \"Noida\"]\nObject.entries(user)  // [[\"name\",\"Devansh\"], [\"city\",\"Noida\"]]</code></pre>\n<p><code>Object.entries</code> is what lets you iterate an object with array methods, and it pairs with <code>Object.fromEntries</code> to go back \u2014 the standard value transform is <code>Object.fromEntries(Object.entries(obj).map(([k, v]) =&gt; [k, f(v)]))</code>.</p>\n<pre><code>\"name\" in user                  // true \u2014 includes inherited properties (Node 6)\nObject.hasOwn(user, \"name\")     // true \u2014 own properties only\nuser.name !== undefined         // WRONG \u2014 a property explicitly set to undefined exists</code></pre>\n<p><code>Object.hasOwn</code> (ES2022) replaces the older <code>user.hasOwnProperty(\"name\")</code>, which breaks if an object has a property literally named <code>hasOwnProperty</code>.</p>\n\n<h4>Optional chaining and nullish coalescing</h4>\n<p>Before 2020, safely reading a nested value meant a chain of guards: <code>user &amp;&amp; user.address &amp;&amp; user.address.city</code>. Optional chaining collapses it:</p>\n<pre><code>const city = user?.address?.city;   // undefined if any link is null/undefined</code></pre>\n<p><code>?.</code> checks whether the value to its left is <code>null</code> or <code>undefined</code>; if so the whole expression short-circuits to <code>undefined</code> instead of throwing. It works for calls (<code>obj.method?.()</code>) and bracket access (<code>arr?.[0]</code>). It guards only against <strong>nullish</strong> values, and should not be sprayed everywhere \u2014 using it on something that should never be missing hides bugs rather than handling them.</p>\n<pre><code>const count = job.applicants || 10;   // gives 10 when applicants is 0 \u2014 WRONG\nconst count = job.applicants ?? 10;   // gives 0 when applicants is 0 \u2014 correct</code></pre>\n<p><code>??</code> supplies the right-hand value only when the left is <code>null</code> or <code>undefined</code>. This is the single most common real bug it was introduced to fix: legitimate <code>0</code>, <code>\"\"</code>, and <code>false</code> values being silently replaced by defaults. There is also <code>??=</code>, which assigns only if the target is nullish.</p>\n\n<h3>In your project</h3>\n<p>This node is load-bearing across three stages of <code>shortlist</code>.</p>\n<p>In <strong>Stage 1</strong>, the <code>||</code> versus <code>??</code> distinction arrives immediately. Job payloads have fields that are legitimately <code>0</code> (applicant count) or <code>\"\"</code> (an empty location meaning \"unspecified\"), and <code>job.salary || \"Not listed\"</code> will override them wrongly.</p>\n<p>In <strong>Stage 2</strong>, the spec warns that when you dedupe later you will find you have been mutating the same <code>raw</code> object from two adapters. That is this node arriving as a bug. The fix is either never mutating <code>raw</code> at all or <code>structuredClone</code>-ing it at the adapter boundary, and choosing between those is a real decision with a real cost.</p>\n<p>In <strong>Stage 5</strong>, deduplication is entirely about identity versus structure. Two records from two boards are structurally similar and never <code>===</code>, so you build a fingerprint string and use it as a <code>Map</code> key \u2014 replacing identity comparison with a structural one you defined yourself. Merging duplicates is the <code>{...a, ...b}</code> pattern, later keys winning.</p>\n<p>In <strong>Stage 6</strong>, <code>structuredClone</code>'s limits become a hard error: sending data to a worker uses the same algorithm, so passing a class instance with methods gets you a <code>DataCloneError</code> or a stripped plain object. That is the moment to realise a <code>Source</code> adapter cannot cross the worker boundary \u2014 only its plain data can.</p>\n\n<h3>In qbank</h3>\n<p>Anything qbank does with Drizzle returns plain objects from the database, and any code that takes one of those rows and mutates it \u2014 adding a computed field, normalising a value \u2014 is mutating a shared object if that reference was handed anywhere else. Worth grepping for places that modify a query result in place, because that is precisely where a hard-to-see bug lives.</p>\n<p>The more interesting one: qbank's Anthropic integration uses forced tool use, receiving structured JSON arguments back from the model. Those arrive as freshly parsed plain objects, and anything done to them before validation \u2014 merging defaults, filling missing fields \u2014 is object manipulation of exactly this kind. When you read that code to explain it, the question to ask is \"does this mutate the model's response or build a new object from it,\" because the answer determines whether the raw response is still intact by the time you log it.</p>\n\n<h3>What an interviewer does with this</h3>\n<p><strong>\"Is JavaScript pass by value or pass by reference?\"</strong> A trap with a precise answer. JavaScript is always pass by value \u2014 but for objects, the value being passed <em>is</em> a reference. So mutating through the parameter affects the caller, while reassigning the parameter does not. Stating both halves, with the two-function example, is a strong signal.</p>\n<p><strong>\"Does <code>const</code> make an object immutable?\"</strong> No. It prevents reassigning the binding. Immutability needs <code>Object.freeze</code>, and even that is shallow.</p>\n<p><strong>\"Why is <code>{a:1} === {a:1}</code> false?\"</strong> Because <code>===</code> on objects compares identity, not structure. Follow up by noting there is no built-in deep equality, and that <code>JSON.stringify</code> comparison is a crude substitute that breaks on key ordering, dates, and anything JSON cannot represent.</p>\n<p><strong>\"How do you deep clone an object?\"</strong> <code>structuredClone</code> \u2014 it handles nesting, <code>Map</code>, <code>Set</code>, <code>Date</code>, and circular references. Then name its limits: functions and class prototypes do not survive. Mentioning that <code>JSON.parse(JSON.stringify(x))</code> is the old approach and listing what it destroys shows you know the trade.</p>\n<p><strong>\"What's the difference between <code>||</code> and <code>??</code>?\"</strong> <code>||</code> falls back on any falsy value; <code>??</code> only on <code>null</code> and <code>undefined</code>. The concrete example lands: a legitimate <code>0</code> or <code>\"\"</code> silently replaced by a default, which is the bug <code>??</code> was added in ES2020 to fix.</p>\n"
    },
    {
      "anchor": "node-3-functions-as-values",
      "title": "Node 3 \u2014 Functions as values",
      "content": "<h3>Where this sits</h3>\n<p>This is the pivot node of the entire language. Node 2 gave you data that sits still; this node makes behaviour itself into data you can move around. Everything from Node 4 onward \u2014 closures, <code>this</code>, prototypes, callbacks, promises, the event loop, every array method, every framework you will ever use \u2014 is built on the single property introduced here.</p>\n\n<h3>Every term, from absolute zero</h3>\n<p><strong>Function</strong> \u2014 a named (or unnamed) package of instructions you can run on demand, possibly many times, possibly with different inputs. Writing it down is <em>declaring</em> or <em>defining</em>; running it is <em>calling</em> or <em>invoking</em>.</p>\n<p><strong>Parameter</strong> \u2014 a name listed in a function's definition, standing in for a value supplied later. In <code>function add(a, b)</code>, <code>a</code> and <code>b</code> are parameters.</p>\n<p><strong>Argument</strong> \u2014 the actual value supplied at the moment of the call. In <code>add(2, 3)</code>, <code>2</code> and <code>3</code> are arguments.</p>\n<p><strong>Return value</strong> \u2014 the value a function hands back using <code>return</code>. A function that never returns explicitly returns <code>undefined</code>.</p>\n<p><strong>Call site</strong> \u2014 the specific place where a function is invoked. Load-bearing in Node 5, because <code>this</code> is determined by the call site rather than by where the function was written.</p>\n<p><strong>Statement</strong> \u2014 a complete instruction that performs an action. Statements do not produce values you can pass around.</p>\n<p><strong>Expression</strong> \u2014 a piece of code that evaluates to a value. The statement/expression distinction separates the two ways of writing a function.</p>\n<p><strong>Function declaration</strong> \u2014 defining a function as a statement, starting with <code>function</code>: <code>function add(a, b) { return a + b; }</code>.</p>\n<p><strong>Function expression</strong> \u2014 defining a function where a value is expected, so the function <em>is</em> the value: <code>const add = function (a, b) { ... };</code>.</p>\n<p><strong>Anonymous function</strong> \u2014 a function with no name of its own.</p>\n<p><strong>First-class</strong> \u2014 a term from programming language theory. A kind of value is \"first-class\" if it can be stored in variables, passed as an argument, returned from a function, and stored in data structures \u2014 treated exactly like any ordinary value. In JavaScript, functions are first-class.</p>\n<p><strong>Higher-order function</strong> \u2014 a function that takes a function as an argument, returns a function, or both. That is the complete definition.</p>\n<p><strong>Callback</strong> \u2014 a function you hand to other code so that code can call it later, at a moment of its choosing.</p>\n<p><strong>Arrow function</strong> \u2014 a function written with <code>=&gt;</code>, introduced in 2015. Shorter, and critically it behaves differently with respect to <code>this</code> (Node 5).</p>\n<p><strong>Implicit return</strong> \u2014 an arrow function written without braces returns its single expression automatically: <code>x =&gt; x * 2</code>.</p>\n<p><strong>Default parameter</strong> \u2014 a value a parameter takes when no argument is supplied.</p>\n<p><strong>Rest parameter</strong> \u2014 a parameter written <code>...name</code> that collects all remaining arguments into a real array.</p>\n<p><strong>Spread</strong> \u2014 the same <code>...</code> syntax used in the opposite direction, expanding an iterable into individual values.</p>\n<p><strong><code>arguments</code></strong> \u2014 an old, array-<em>like</em> object automatically available inside <code>function</code>-keyword functions. Not a real array, does not exist in arrow functions, and fully replaced by rest parameters.</p>\n<p><strong>Pure function</strong> \u2014 always returns the same output for the same input and changes nothing outside itself. Not enforced by JavaScript, but a useful category.</p>\n<p><strong>Side effect</strong> \u2014 anything a function does beyond computing its return value: mutating an object, writing to disk, printing, making a request.</p>\n<p><strong>IIFE</strong> \u2014 Immediately Invoked Function Expression, written <code>(function(){ ... })()</code>. Pronounced \"iffy.\" Historically important; Node 13's material.</p>\n\n<h3>The history \u2014 why this exists at all</h3>\n<p>The idea that a function could be an ordinary value did not originate with JavaScript, and knowing where it came from explains why JavaScript has it while its supposed namesake Java did not.</p>\n<p>The lineage runs back to <strong>Lisp</strong>, created by John McCarthy in 1958, and to the mathematical system it was built on, <strong>lambda calculus</strong>, devised by Alonzo Church in the 1930s. Church's system had exactly one kind of thing: functions. Functions took functions and returned functions, and everything else was encoded out of them. Lisp brought that into a real language, and a family descended from it \u2014 Scheme, ML, Haskell \u2014 treated functions as ordinary values throughout.</p>\n<p>Meanwhile the mainstream languages of the 1980s and 90s took the opposite path. In C you could pass a <em>pointer</em> to a function, with heavy restrictions and no ability to create new functions at runtime. In Java, as of 1995, you could not pass a function at all \u2014 you passed an object with a method on it, which is why Java code of that era is full of one-method interfaces and anonymous inner classes.</p>\n<p>Brendan Eich's original brief at Netscape was, by his own repeated account, to make something that \"looked like Java\" for marketing reasons \u2014 but he had been hired with the expectation that he would bring <strong>Scheme</strong> to the browser, and his design instincts came from Scheme and from <strong>Self</strong>, a research language with an unusual object model. So JavaScript ended up with Java-ish curly-brace syntax wrapped around a Scheme-ish core. First-class functions and closures came from Scheme. The prototype object model (Node 6) came from Self. The <code>for</code> loops and the <code>+</code> operator came from C and Java.</p>\n<p>That mismatch between surface and substance is why JavaScript was underestimated for its first decade. It looked like a toy version of Java, so people wrote it like a toy version of Java, and the genuinely powerful part sat unused. The turning point was cultural: Douglas Crockford's essay \"JavaScript: The World's Most Misunderstood Programming Language\" (2001) and his book <em>JavaScript: The Good Parts</em> (2008) argued that the language's functions were its best feature and were being ignored. Around the same time <strong>jQuery</strong> (2006) made callback-passing the normal way to write browser code, and <strong>Node.js</strong> (2009) built an entire server platform on the assumption that you pass functions to things.</p>\n<p>One direct consequence you will feel constantly: because functions are values, JavaScript never needed a separate mechanism for \"a chunk of behaviour to run later.\" Every asynchronous feature the language grew \u2014 callbacks, promises, <code>async</code>/<code>await</code>, event handlers \u2014 is built out of passing functions around. Languages without first-class functions had to invent something else. JavaScript already had the answer before it had the problem, which is why the second half of this chain works at all.</p>\n\n<h3>The mechanism \u2014 traced</h3>\n\n<h4>Two ways to write one, and why it matters</h4>\n<pre><code>// Declaration \u2014 a statement\nfunction add(a, b) { return a + b; }\n\n// Expression \u2014 a value being produced and assigned\nconst add = function (a, b) { return a + b; };\n\n// Arrow function expression\nconst add = (a, b) =&gt; a + b;</code></pre>\n<p>Two differences matter. The first is <strong>hoisting</strong> (Node 4): a function <em>declaration</em> is fully available before the line it appears on. A function <em>expression</em> assigned to a <code>const</code> is not.</p>\n<pre><code>declared(); // works\nfunction declared() {}\n\nexpressed(); // ReferenceError: Cannot access 'expressed' before initialization\nconst expressed = function () {};</code></pre>\n<p>The second is that an expression can appear anywhere a value can \u2014 inside an array, as an argument, as a return value \u2014 while a declaration cannot, because it is a statement.</p>\n\n<h4>The property itself</h4>\n<pre><code>const double = (n) =&gt; n * 2;\n\nconst fns = [double, (n) =&gt; n + 1];                // stored in an array\nconst registry = { double, half: (n) =&gt; n / 2 };   // stored in an object\n\nfunction applyTwice(fn, value) {                   // received as an argument\n  return fn(fn(value));\n}\n\nfunction makeMultiplier(factor) {                  // returned from a function\n  return (n) =&gt; n * factor;\n}</code></pre>\n<p>Nothing here is special syntax. A function is a value in exactly the sense that <code>5</code> and <code>\"hello\"</code> are values. It is, specifically, an object \u2014 so it can carry properties of its own:</p>\n<pre><code>function scan() {}\nscan.description = \"Fetches all job boards\";\ntypeof scan;   // \"function\" \u2014 the one carve-out mentioned in Node 2\nscan.name;     // \"scan\"\nscan.length;   // 0 \u2014 number of declared parameters</code></pre>\n<p><code>fn.length</code> counts declared parameters <em>before</em> the first default or rest parameter.</p>\n\n<h4>Higher-order functions in practice</h4>\n<pre><code>jobs.map(normalise)\njobs.filter(j =&gt; j.remote)\njobs.sort((a, b) =&gt; b.score - a.score)\nsetTimeout(refresh, 1000)\nbutton.addEventListener(\"click\", handleClick)</code></pre>\n<p>Every one is a higher-order function, and none could exist in a language without first-class functions. That is what makes this the pivot node: <code>map</code> is not a special language feature, it is an ordinary method that happens to accept a function, and you could write it yourself in four lines.</p>\n<p>The <em>returning</em> direction is less familiar and more powerful:</p>\n<pre><code>function withRetry(fn, attempts = 3) {\n  return async function (...args) {\n    for (let i = 0; i &lt; attempts; i++) {\n      try {\n        return await fn(...args);\n      } catch (err) {\n        if (i === attempts - 1) throw err;\n      }\n    }\n  };\n}\n\nconst fetchWithRetry = withRetry(fetchBoard);</code></pre>\n<p><code>withRetry</code> takes a function and hands back a <em>new</em> function with retry behaviour wrapped around it. The original is untouched, and the wrapper works for any function. This shape \u2014 a function that decorates another function \u2014 is exactly what Express middleware, Fastify hooks, React higher-order components, and Python decorators all are.</p>\n\n<h4>Arrow functions: what actually differs</h4>\n<pre><code>const a = function (x) { return x * 2; };\nconst b = (x) =&gt; { return x * 2; };\nconst c = (x) =&gt; x * 2;    // implicit return\nconst d = x =&gt; x * 2;      // parens optional for exactly one parameter\nconst e = () =&gt; 42;        // parens required for zero parameters</code></pre>\n<ol>\n<li><strong>No own <code>this</code>.</strong> An arrow uses the <code>this</code> of the surrounding code, resolved lexically like any ordinary variable. Node 5 is entirely about why this fixed a whole class of bug.</li>\n<li><strong>No <code>arguments</code> object.</strong> Use rest parameters instead.</li>\n<li><strong>Cannot be called with <code>new</code>.</strong> Arrows are not constructors and throw if you try.</li>\n<li><strong>No <code>prototype</code> property.</strong> A consequence of point 3.</li>\n</ol>\n<p>Two syntax traps. Returning an object literal implicitly requires parentheses, because the parser reads <code>{</code> as the start of a function body:</p>\n<pre><code>const makeJob = (title) =&gt; { title };     // returns undefined \u2014 {title} is a block\nconst makeJob = (title) =&gt; ({ title });   // returns { title: title }</code></pre>\n<pre><code>const double = (n) =&gt; { n * 2; };         // returns undefined \u2014 computed and discarded\nconst double = (n) =&gt; { return n * 2; };</code></pre>\n<p>Practical guidance: arrows for callbacks and short transformations; <code>function</code> for object methods and anything called with <code>new</code>.</p>\n\n<h4>Parameters: defaults, rest, spread</h4>\n<p><strong>Default parameters</strong> (2015) fire only when the argument is <code>undefined</code>:</p>\n<pre><code>function greet(name = \"there\") { return `Hi ${name}`; }\ngreet();            // \"Hi there\"\ngreet(undefined);   // \"Hi there\"\ngreet(null);        // \"Hi null\" \u2014 null is a real value, not an absence\ngreet(\"\");          // \"Hi \" \u2014 empty string is a real value</code></pre>\n<p>Defaults are evaluated at call time, left to right, and can reference earlier parameters:</p>\n<pre><code>function paginate(page = 1, perPage = 20, offset = (page - 1) * perPage) {}</code></pre>\n<p><strong>Rest parameters</strong> collect everything remaining into a genuine array. Must be last, only one allowed:</p>\n<pre><code>function sum(...nums) {\n  return nums.reduce((total, n) =&gt; total + n, 0);\n}\nsum(1, 2, 3);   // 6 \u2014 nums is a real array with real methods</code></pre>\n<p>This replaced <code>arguments</code>, which was array-<em>like</em>: a <code>length</code> and numeric indices but none of the array methods. Rest parameters are a real array, work in arrow functions, and let you collect only <em>some</em> arguments: <code>function log(level, ...messages) {}</code>.</p>\n<p><strong>Spread</strong> is the same three dots doing the reverse job. The rule is positional: <code>...</code> in a parameter list or on the left of an assignment <strong>collects</strong>; <code>...</code> at a call site or inside a literal <strong>expands</strong>.</p>\n<pre><code>const pair = [2, 3];\nadd(...pair);                          // expands to add(2, 3)\nMath.max(...scores);\nconst all = [...boards, ...extras];\nconst merged = { ...defaults, ...overrides };</code></pre>\n<p>Spread works on anything iterable (Node 7), which is why it works on strings, <code>Map</code>, and <code>Set</code> as well as arrays.</p>\n\n<h4>Functions as arguments: the shape of a callback</h4>\n<pre><code>setTimeout(refresh, 1000);     // pass the function itself\nsetTimeout(refresh(), 1000);   // BUG \u2014 calls it now, passes its return value</code></pre>\n<p>A direct consequence of functions being values: <code>refresh</code> is the function, <code>refresh()</code> is the result of running it. In the second line <code>refresh</code> runs immediately and <code>setTimeout</code> receives whatever it returned \u2014 usually <code>undefined</code>, which it then tries to call and silently does nothing. To pass arguments to a callback, wrap it: <code>setTimeout(() =&gt; refresh(boardId), 1000)</code>.</p>\n\n<h3>In your project</h3>\n<p><strong>Stage 1</strong> has you writing <code>normalise(rawJob)</code> and <code>formatJob(job)</code> and passing them to <code>map</code> \u2014 higher-order function usage on the first day.</p>\n<p><strong>Stage 2</strong> is where callbacks start to bite. Before you lose <code>this</code>, you will pass <code>scan</code> versus <code>scan()</code> wrongly at least once, and watching a source silently produce <code>undefined</code> is a cheap version of the lesson.</p>\n<p><strong>Stage 3</strong> is explicitly <em>about</em> this node. You build <code>withRetry(fn, opts)</code> and <code>createLimiter(maxConcurrent)</code>, both taking a function and returning a wrapped function holding private state. Having written them you will recognise the shape everywhere: Express middleware handed to a framework, Fastify hooks, BullMQ's processor function.</p>\n<p><strong>Stage 8</strong> takes it furthest: <code>effect(fn)</code> stores a function so it can be re-run whenever dependencies change \u2014 first-class functions used as the core of an architecture rather than as a convenience.</p>\n\n<h3>In qbank</h3>\n<p>qbank is built almost entirely out of this node. Fastify's route handlers are functions you hand to the framework, which calls them when a matching request arrives \u2014 a callback the framework never invokes until the moment comes. Fastify's plugin system takes functions that receive the instance and register things on it. BullMQ's worker takes a processor function it calls once per job.</p>\n<p>The one most worth understanding precisely is the Anthropic tool-use loop. Forced tool use means the model returns a request to call a named tool with structured arguments, and your code dispatches to the right handler. That dispatch is a lookup from a tool name to a function \u2014 almost certainly an object or <code>Map</code> whose values are functions \u2014 which is exactly \"functions stored in a data structure.\" The sentence you want when explaining it: \"the tool registry maps a name to a handler function, and the loop looks up the handler and calls it with the model's arguments.\"</p>\n\n<h3>What an interviewer does with this</h3>\n<p><strong>\"What does it mean that functions are first-class in JavaScript?\"</strong> A weak answer restates the phrase. A strong one gives the four capabilities \u2014 stored in variables, passed as arguments, returned from functions, held in data structures \u2014 then makes it concrete: this is why <code>map</code> can exist as an ordinary method rather than special syntax, and why every async mechanism is built from passing functions around.</p>\n<p><strong>\"Declaration versus expression?\"</strong> Declarations are statements, hoisted with their body, callable before their line. Expressions produce a value and are only available after the assignment executes. An expression can go anywhere a value can; a declaration cannot.</p>\n<p><strong>\"Arrow function versus regular function?\"</strong> Lead with <code>this</code>: an arrow has no own <code>this</code> and resolves it lexically, which is why arrows fixed the callback problem. Then no <code>arguments</code>, cannot be used with <code>new</code>, no <code>prototype</code>. Answering only \"arrows are shorter\" is the weak version.</p>\n<p><strong>\"What's a higher-order function? Give one you've written.\"</strong> Definition first, then a real one. <code>withRetry</code> from <code>shortlist</code> works well because it takes a function and returns a wrapped function, and you can explain why that beats putting retry logic inside every caller.</p>\n<p><strong>\"Rest versus spread?\"</strong> Same three dots, opposite roles. Rest appears in a parameter list or destructuring target and <em>collects</em>; spread appears at a call site or inside a literal and <em>expands</em>. Adding that rest replaced the array-like <code>arguments</code> object, which does not exist in arrow functions, shows depth.</p>\n"
    },
    {
      "anchor": "node-4-scope-hoisting-closures",
      "title": "Node 4 \u2014 Scope, hoisting, and closures",
      "content": "<h3>Where this sits</h3>\n<p>Node 3 made functions into values that can be passed elsewhere and called later. The instant that happens, \"which variables can this function see, and what are their values right now?\" stops being obvious. This node answers that, and the answer \u2014 closures \u2014 is not an optional feature but an unavoidable consequence of the two things you already have. Node 5 exists because <code>this</code> is the single name that disobeys everything here. Node 15's memory leaks are all closures holding things alive.</p>\n\n<h3>Every term, from absolute zero</h3>\n<p><strong>Scope</strong> \u2014 the region of your program where a particular name is visible and usable.</p>\n<p><strong>Block</strong> \u2014 any section of code between curly braces. The body of an <code>if</code>, a <code>for</code> loop, a <code>while</code> loop, or just a bare pair of braces.</p>\n<p><strong>Global scope</strong> \u2014 the outermost region, outside every function and block. Names declared there are visible everywhere.</p>\n<p><strong>Function scope</strong> \u2014 the region inside a function. Names declared with <code>var</code> inside a function are visible throughout that entire function, and not outside it.</p>\n<p><strong>Block scope</strong> \u2014 the region inside a pair of curly braces. Names declared with <code>let</code> or <code>const</code> are visible only inside that block. Added in 2015; before that JavaScript had no block scope at all.</p>\n<p><strong>Scope chain</strong> \u2014 the ordered list of scopes the language searches when it meets a name: current scope, then the one containing it, out to global. First match wins; no match is a <code>ReferenceError</code>.</p>\n<p><strong>Lexical</strong> \u2014 from the Greek for \"word,\" meaning \"relating to the text as written.\" <strong>Lexical scoping</strong> means a name's scope is determined by <em>where the code physically sits in the source</em>, decided when written, not by anything that happens at runtime. The alternative, <strong>dynamic scoping</strong>, would resolve names based on who called whom; almost no modern language does this \u2014 except, confusingly, JavaScript's <code>this</code>.</p>\n<p><strong>Shadowing</strong> \u2014 declaring a name in an inner scope that already exists outside. The inner one shadows the outer for the duration of the inner scope; the outer is untouched.</p>\n<p><strong>Declaration</strong> \u2014 the part that introduces a name: <code>let count</code>. <strong>Initialisation</strong> is giving it a value. <code>let count = 0</code> does both, but they are separate steps and the gap between them is what the temporal dead zone is about.</p>\n<p><strong>Hoisting</strong> \u2014 the engine processing all declarations in a scope <em>before</em> running any of that scope's code. Nothing physically moves; it describes the order in which things happen.</p>\n<p><strong>Temporal dead zone (TDZ)</strong> \u2014 the stretch from the start of a scope to the line where a <code>let</code>/<code>const</code> is initialised. The name exists but cannot be touched; access throws. \"Temporal\" because it is defined by time during execution, not position in the file.</p>\n<p><strong><code>ReferenceError</code></strong> \u2014 thrown when you use a name that does not exist, or one in its temporal dead zone.</p>\n<p><strong>Stack frame</strong> \u2014 the bundle created when a function is called: its parameters, local variables, and where to return. Normally discarded when the function returns. Closures are the exception.</p>\n<p><strong>Closure</strong> \u2014 a function bundled together with the scope it was created in, retaining access to that scope's variables even after that scope has finished executing.</p>\n<p><strong>Free variable</strong> \u2014 a variable a function uses but did not declare and did not receive as a parameter. It must come from an enclosing scope. Free variables are exactly what a closure closes over.</p>\n<p><strong>Encapsulation</strong> \u2014 keeping data private, reachable only through controlled operations. Closures were JavaScript's only way to do this for twenty years.</p>\n<p><strong>Memoisation</strong> \u2014 caching a function's results so repeating an input returns the stored answer. From \"memo,\" not a misspelling of memorisation.</p>\n<p><strong>IIFE</strong> \u2014 Immediately Invoked Function Expression, <code>(function(){ ... })()</code>. A function defined and called on the spot purely to create a scope.</p>\n\n<h3>The history \u2014 why this exists at all</h3>\n<p>Closures were not invented for JavaScript. They came from <strong>Scheme</strong>, and behind Scheme from work in the 1960s and 70s on making lambda calculus implementable. The problem was precisely this: if a function can be returned from another function, and it refers to variables from the function that created it, what happens to those variables when the creating function returns?</p>\n<p>The naive answer \u2014 throw them away \u2014 makes returned functions useless. This was called the <strong>funarg problem</strong> and was a genuine open issue in language design in the 1960s. The solution, worked out properly in Scheme in 1975 by Gerald Sussman and Guy Steele, was to keep the variables alive as long as any function referring to them is alive. The function and its captured environment travel together as one bundle. That bundle is a closure.</p>\n<p>Brendan Eich brought this from Scheme into JavaScript in 1995, and it is arguably the most valuable thing he brought. But he brought it into a language whose <em>variable declaration</em> mechanism was much cruder, and that mismatch produced twenty years of confusion.</p>\n<p>Original JavaScript had one way to declare a variable: <code>var</code>, which was <strong>function-scoped</strong>. A <code>var</code> inside an <code>if</code> or a <code>for</code> belonged to the entire enclosing function. Worse, <code>var</code> was <strong>hoisted with an undefined initialisation</strong>: reading it before its declaration line gave <code>undefined</code> rather than an error, silently. Combined with Node 1's coercion, an <code>undefined</code> could travel a long way before causing visible damage.</p>\n<p>The two together produced the notorious loop-with-callbacks bug traced below. Developers worked around missing block scope using closures themselves \u2014 wrapping code in an immediately invoked function to manufacture a scope. That is the <strong>IIFE</strong>, and by the late 2000s every jQuery plugin opened with <code>(function($){</code> and closed with <code>})(jQuery)</code>.</p>\n<p><strong>ES2015 fixed it properly</strong> with <code>let</code> and <code>const</code> and genuine block scope. But \u2014 the recurring pattern from Node 0 \u2014 <code>var</code> could not be removed. So the language now has three declaration keywords: one broken one kept for compatibility and two correct ones. And the fix went further: <code>let</code> and <code>const</code> were deliberately given the <strong>temporal dead zone</strong>, so reading them before initialisation throws loudly rather than yielding <code>undefined</code> quietly. The committee chose a noisy error over a quiet wrong answer \u2014 a design principle worth naming, because it recurs.</p>\n\n<h3>The mechanism \u2014 traced</h3>\n\n<h4>The scope chain, resolved</h4>\n<pre><code>const appName = \"shortlist\";          // global scope\n\nfunction scanBoards(sources) {         // function scope\n  const started = Date.now();\n\n  for (const source of sources) {      // block scope (per iteration)\n    const label = source.name;\n\n    function log(message) {            // nested function scope\n      console.log(appName, label, message, Date.now() - started);\n    }\n\n    log(\"scanning\");\n  }\n}</code></pre>\n<p>When <code>log</code> needs <code>appName</code>, the engine searches outward: its own scope, the <code>for</code> block, <code>scanBoards</code>, then global \u2014 found. That search is the <strong>scope chain</strong>, and it goes strictly outward. Visibility is one-directional.</p>\n<p><strong>Lexical</strong> means this chain is fixed by where the code was written. You can determine it by reading the file. Nothing at runtime changes it.</p>\n<pre><code>const name = \"outer\";\nfunction show() {\n  const name = \"inner\";   // shadows the outer name\n  console.log(name);      // \"inner\" \u2014 search stops at the first match\n}\nshow();\nconsole.log(name);        // \"outer\" \u2014 untouched</code></pre>\n\n<h4>The three declarations, precisely</h4>\n<pre><code>function demo() {\n  if (true) {\n    var a = 1;\n    let b = 2;\n  }\n  console.log(a);   // 1 \u2014 var ignored the block, belongs to the function\n  console.log(b);   // ReferenceError \u2014 b existed only inside the block\n}</code></pre>\n<p><code>var</code> is function-scoped; <code>let</code>/<code>const</code> are block-scoped. That is the primary difference and the reason <code>var</code> is no longer used.</p>\n<p>Two secondary differences. <code>var</code> can be redeclared in the same scope without complaint, which hides typos; <code>let</code>/<code>const</code> throw a <code>SyntaxError</code>. And <code>var</code> at the top level of a script attaches to the global object as a property (<code>window.x</code> in a browser), while <code>let</code>/<code>const</code> do not \u2014 a real source of accidental collisions between scripts.</p>\n<p><code>const</code> prevents <strong>reassignment of the binding</strong>, not mutation of the value. And a <code>const</code> must be initialised at declaration; <code>const x;</code> is a <code>SyntaxError</code>.</p>\n\n<h4>Hoisting, traced honestly</h4>\n<p>Before running any code in a scope, the engine registers every declaration it finds. What happens next differs by kind.</p>\n<p><strong>Function declarations</strong> are registered complete, with their body:</p>\n<pre><code>greet();                      // \"hi\" \u2014 works\nfunction greet() { console.log(\"hi\"); }</code></pre>\n<p><strong><code>var</code> declarations</strong> are registered and initialised to <code>undefined</code>:</p>\n<pre><code>console.log(count);   // undefined \u2014 not an error\nvar count = 5;</code></pre>\n<p>Only the <em>declaration</em> moves conceptually \u2014 the <em>assignment</em> stays exactly where you wrote it. This is the piece people get wrong.</p>\n<p><strong><code>let</code> and <code>const</code></strong> are registered but deliberately left uninitialised, in the temporal dead zone:</p>\n<pre><code>console.log(total);   // ReferenceError: Cannot access 'total' before initialization\nlet total = 5;</code></pre>\n<p>The message is precise: <em>cannot access</em>, not <em>is not defined</em>. The name exists but touching it before its initialisation line is forbidden. Compare a name that genuinely does not exist: <code>ReferenceError: nothing is not defined</code>. Two different messages, two different situations \u2014 distinguishing them is a cheap signal of real understanding.</p>\n<p>So the common claim \"<code>let</code> and <code>const</code> are not hoisted\" is <strong>false</strong>. They are hoisted; the difference is what happens when you touch them early.</p>\n<p>One more subtlety: function <em>expressions</em> are not hoisted as functions, because what is hoisted is the variable:</p>\n<pre><code>run();                     // TypeError: run is not a function\nvar run = function () {};  // var run was hoisted as undefined; undefined() throws</code></pre>\n<p>Note the error is a <code>TypeError</code>, not a <code>ReferenceError</code> \u2014 <code>run</code> exists and holds <code>undefined</code>, and calling <code>undefined</code> is a type error.</p>\n\n<h4>Closures, from first principles</h4>\n<p>Combine two things you already have. Functions are values (Node 3), so a function can outlive the scope that created it. Scoping is lexical, so that function's names resolve to the scope where it was <em>written</em>. The language has no choice: it must keep that scope's variables alive as long as the function referring to them is alive.</p>\n<pre><code>function makeCounter() {\n  let count = 0;\n  return () =&gt; ++count;\n}\n\nconst next = makeCounter();\nnext();   // 1\nnext();   // 2</code></pre>\n<p>Calling <code>makeCounter()</code> creates a scope containing <code>count</code>. It creates an arrow referring to <code>count</code> \u2014 a <strong>free variable</strong>. It returns that arrow, and <code>makeCounter</code> finishes. Normally its stack frame would be discarded, but it cannot be, because the returned function still refers to <code>count</code>. So the variable survives, reachable only through that function.</p>\n<pre><code>const a = makeCounter();\nconst b = makeCounter();\na(); a(); // 1, 2\nb();      // 1 \u2014 completely independent</code></pre>\n<p>Each <em>call</em> creates a fresh scope with its own <code>count</code>. Closures are created per invocation, not per function definition.</p>\n<p>And <code>count</code> is genuinely unreachable from outside. No property access, no method, nothing. That is real <strong>encapsulation</strong>, achieved with nothing but scoping rules.</p>\n\n<h4>The loop bug, and why let fixes it</h4>\n<pre><code>for (var i = 0; i &lt; 3; i++) {\n  setTimeout(() =&gt; console.log(i), 0);\n}\n// prints: 3, 3, 3</code></pre>\n<p><code>var i</code> is function-scoped, so there is exactly <strong>one</strong> <code>i</code>. Three arrows are created, all closing over that same variable. <code>setTimeout</code> schedules them for later (Node 9). The loop runs to completion, incrementing <code>i</code> to <code>3</code> \u2014 which is what ends the loop. <em>Then</em> the callbacks run, each reading the one shared <code>i</code>.</p>\n<p>Nothing is broken. The closures captured the <em>variable</em>, not a snapshot of its value.</p>\n<pre><code>for (let i = 0; i &lt; 3; i++) {\n  setTimeout(() =&gt; console.log(i), 0);\n}\n// prints: 0, 1, 2</code></pre>\n<p><code>let</code> in a <code>for</code> header is special-cased in the specification: the engine creates a <strong>fresh binding for each iteration</strong> and copies the current value in. Three separate <code>i</code> variables, each captured by one closure.</p>\n<p>Before <code>let</code>, the fix was an IIFE \u2014 closures working around the absence of block scope:</p>\n<pre><code>for (var i = 0; i &lt; 3; i++) {\n  (function (captured) {\n    setTimeout(() =&gt; console.log(captured), 0);\n  })(i);\n}\n// prints: 0, 1, 2</code></pre>\n<p>Each IIFE call creates a scope whose parameter holds a copy of <code>i</code> at that moment. <code>let</code>'s per-iteration binding is not magic \u2014 it is the same trick, done by the language.</p>\n\n<h4>What closures are actually used for</h4>\n<p><strong>Private state</strong>, as in <code>makeCounter</code>. Before <code>#private</code> class fields (Node 6), this was the only way.</p>\n<p><strong>Function factories</strong> \u2014 a function that builds specialised functions.</p>\n<p><strong>Memoisation</strong> \u2014 a cache that persists between calls but belongs to nobody else:</p>\n<pre><code>function memoise(fn) {\n  const cache = new Map();\n  return (arg) =&gt; {\n    if (!cache.has(arg)) cache.set(arg, fn(arg));\n    return cache.get(arg);\n  };\n}</code></pre>\n<p><code>cache</code> lives as long as the returned function does. Genuinely useful \u2014 and, as Node 15 shows, exactly how an unbounded memory leak is built.</p>\n<p><strong>Callbacks that remember context.</strong> Every event handler, every <code>setTimeout</code>, every promise <code>.then</code> is a closure over whatever was in scope when you wrote it. This is why a React <code>useState</code> value can be \"stale\" inside a callback: the callback closed over the variable as it existed on that render, and a later render created a <em>different</em> variable.</p>\n<p><strong>The module pattern</strong> \u2014 an IIFE returning an object whose methods close over private variables. Node 13's history.</p>\n\n<h3>In your project</h3>\n<p>Stage 3 of <code>shortlist</code> is built directly on this node. <code>withRetry(fn, opts)</code> returns a wrapper that tracks attempts \u2014 a counter that cannot be global (concurrent calls would corrupt each other) and should not be a parameter (the caller should not manage it). It lives in the closure, one per wrapped function, invisible from outside. Same for <code>createLimiter(maxConcurrent)</code>, closing over a queue and an in-flight count.</p>\n<p>The debounce you write in Stage 7 for the search box is the same shape \u2014 closing over a timer handle so each keystroke cancels the previous pending call.</p>\n<p>Stage 9 is where closures turn against you. Two of the four planted leaks are closure leaks: the unremoved listener keeps its handler alive, and the handler's closure keeps alive <em>everything the enclosing scope held</em>. The unbounded memo cache is the <code>memoise</code> function above with no eviction. Node 15 is not a separate subject \u2014 a leak is a closure or reference you forgot you created.</p>\n\n<h3>In qbank</h3>\n<p>Anywhere qbank builds a Fastify plugin or route handler that references something from the enclosing module or factory \u2014 a database client, a config object, a logger \u2014 that handler is a closure over it. The reason it can reach the database without receiving it as a parameter is exactly this mechanism, and being able to say that sentence about your own code is the fluency gap you are closing.</p>\n<p>Second: if there is any caching in qbank \u2014 a memoised lookup, a rate limiter, a prepared-statement holder \u2014 check whether it is bounded. An unbounded cache in a long-running Fastify process is the exact leak shape from Stage 9.</p>\n\n<h3>What an interviewer does with this</h3>\n<p><strong>\"What is a closure?\"</strong> Weak: \"a function inside a function.\" Strong: a function bundled with the scope it was created in, retaining access to that scope's variables after the outer function returned \u2014 and why it must work that way: functions are values that can outlive their creating scope, and scoping is lexical, so the language has no choice. Follow with <code>makeCounter</code> and the observation that two calls produce two independent counters.</p>\n<p><strong>\"<code>var</code> vs <code>let</code> vs <code>const</code>?\"</strong> Lead with scope, then hoisting: all three are hoisted, but <code>var</code> is initialised to <code>undefined</code> while <code>let</code>/<code>const</code> sit in the TDZ and throw. Then <code>const</code> prevents rebinding, not mutation. Saying \"<code>let</code> and <code>const</code> aren't hoisted\" is the common wrong answer and interviewers listen for it.</p>\n<p><strong>\"What does this print?\"</strong> \u2014 the <code>var</code> loop with <code>setTimeout</code>. Answer <code>3, 3, 3</code>, explain one shared variable and three closures over it, then the <code>let</code> version as a fresh binding per iteration. For full marks, mention the pre-2015 IIFE fix, which shows you know what <code>let</code> is doing rather than just that it works.</p>\n<p><strong>\"How do you make a variable private?\"</strong> Closures, historically, and still the answer for functions and modules. For classes, <code>#private</code> fields since 2022. Noting the underscore convention was never enforcement, only a request, is the detail that lands.</p>\n<p><strong>\"Why might a value be stale inside a callback?\"</strong> Because the callback closed over the variable as it existed when created. In React, a later render creates new variables and an old callback still references the old ones. This tests whether you can apply closures to diagnose a real bug rather than recite the definition.</p>\n"
    },
    {
      "anchor": "node-5-this-and-call-context",
      "title": "Node 5 \u2014 this, and how a function is called",
      "content": "<h3>Where this sits</h3>\n<p>Node 4 established that every name in JavaScript resolves lexically \u2014 by where the code is written. This node is about the single exception. <code>this</code> appears constantly in real code, is used by every class-based codebase and every framework you will touch, and follows a completely different rule from everything else. Node 6 depends on it, because methods shared through prototypes need a way to know which object they are working on.</p>\n\n<h3>Every term, from absolute zero</h3>\n<p><strong>Method</strong> \u2014 a function stored as a property of an object, so it can be called as <code>obj.doThing()</code>. In JavaScript a method is not a special kind of thing; it is just a function that happens to live on an object.</p>\n<p><strong>Receiver</strong> \u2014 the object a method is called on. In <code>user.greet()</code>, <code>user</code> is the receiver. The clearest name for what <code>this</code> refers to.</p>\n<p><strong>Call site</strong> \u2014 the exact place a function is invoked, and specifically the <em>form</em> of that invocation: whether there was a dot, a <code>new</code>, or a <code>call</code>/<code>apply</code>.</p>\n<p><strong>Binding</strong> \u2014 associating a <code>this</code> value with a function for the duration of a call.</p>\n<p><strong>Implicit binding</strong> \u2014 <code>this</code> determined automatically by a dot at the call site.</p>\n<p><strong>Explicit binding</strong> \u2014 <code>this</code> set deliberately using <code>call</code>, <code>apply</code>, or <code>bind</code>.</p>\n<p><strong>Default binding</strong> \u2014 what <code>this</code> falls back to when nothing else applies: <code>undefined</code> in strict mode, the global object otherwise.</p>\n<p><strong>Global object</strong> \u2014 the object at the top of the environment holding the built-in globals. <code>window</code> in a browser, <code>global</code> in Node, <code>globalThis</code> portably. Supplied by the runtime, not the language (Node 17).</p>\n<p><strong>Strict mode</strong> \u2014 an opt-in stricter dialect introduced in ES5 (2009), activated by <code>\"use strict\"</code>. It turns silent failures into errors and changes the default <code>this</code>. ES Modules and class bodies are strict automatically.</p>\n<p><strong>Sloppy mode</strong> \u2014 the informal name for non-strict mode, the original behaviour kept for backward compatibility.</p>\n<p><strong>Constructor</strong> \u2014 a function intended to be called with <code>new</code> to produce a new object.</p>\n<p><strong>Detached method</strong> \u2014 a method separated from its object, usually by assignment or by being passed as an argument, so the dot is gone at the call site.</p>\n<p><strong>Lexical <code>this</code></strong> \u2014 the behaviour of arrow functions, where <code>this</code> is not determined by the call site at all but inherited from the enclosing scope, exactly as an ordinary variable would be.</p>\n\n<h3>The history \u2014 why this exists at all</h3>\n<p>Object-oriented languages of the 1980s and 90s \u2014 Smalltalk, C++, Java \u2014 all had some version of this idea. You write a method once, on a class, and it runs against many different instances. The method body needs a way to say \"the particular object I am currently operating on.\" In those languages the mechanism is structural: a method belongs to a class, an object is an instance, and calling a method binds <code>this</code> to that object. The connection is permanent.</p>\n<p>JavaScript did not have classes. It had objects that were bags of properties (Node 2) and functions that were ordinary values (Node 3). A \"method\" was simply a function stored in a property. And here is the consequence: <strong>because the function is just a value sitting in a property, there is no permanent connection between the function and the object.</strong> The same function can be stored on ten objects, pulled into a variable, or passed elsewhere. Nothing in the function records which object it belongs to, because it belongs to none.</p>\n<p>So JavaScript needed a rule for determining <code>this</code> at the moment of the call. The rule Eich chose: look at <em>how the function was called</em>. If there was a dot, the thing to the left of the dot is <code>this</code>.</p>\n<p>That worked fine for the small scripts people wrote in 1995. It became a problem when JavaScript grew callbacks, because a callback is precisely a function separated from its context and handed to someone else to call later. The moment you write <code>setTimeout(user.greet, 100)</code>, the dot is gone and <code>this</code> is lost.</p>\n<p>The workarounds accumulated. In the 2000s the standard fix was to capture <code>this</code> in an ordinary variable so lexical scoping could carry it: <code>const self = this</code>. You still see <code>var self = this</code> in older code, and now you know exactly what it is doing \u2014 turning <code>this</code> into a normal variable so a closure can capture it.</p>\n<p>ES5 (2009) added <code>Function.prototype.bind</code>, making the fix explicit. ES2015 added <strong>arrow functions</strong>, which solved it properly by making <code>this</code> lexical inside them. That was the real fix \u2014 not a workaround for the rule but a function form that opts out of the rule entirely. Understanding that arrow functions exist primarily to fix <code>this</code>, and only incidentally to be shorter, is the most useful reframing in this node.</p>\n<p>One honest note: the specification does not use the phrase \"the four binding rules.\" That framing comes from Kyle Simpson's <em>You Don't Know JS</em> and has become standard because it is genuinely clarifying. The underlying behaviour is exactly what the spec describes; the four-rule presentation is a teaching device.</p>\n\n<h3>The mechanism \u2014 traced</h3>\n\n<h4>The rule, in one sentence</h4>\n<p><strong><code>this</code> is determined by how the function was called, not by where it was written.</strong> Everything else is a consequence. There are four call patterns.</p>\n\n<h4>Pattern 1 \u2014 plain function call (default binding)</h4>\n<pre><code>function whoAmI() { console.log(this); }\nwhoAmI();</code></pre>\n<p>No dot, no <code>new</code>, no explicit binding. In <strong>strict mode</strong> <code>this</code> is <code>undefined</code>; in <strong>sloppy mode</strong> it falls back to the global object.</p>\n<p>That sloppy-mode fallback was a genuine design mistake: a bug silently succeeds and starts reading and writing global state instead of failing. Strict mode's <code>undefined</code> is the correction \u2014 the failure becomes a loud <code>TypeError</code> the moment you touch a property.</p>\n<p>Since ES Modules and class bodies are strict automatically, in modern code expect <code>undefined</code>. Which means <code>TypeError: Cannot read properties of undefined (reading 'name')</code> is very often a <code>this</code> problem, not a data problem.</p>\n\n<h4>Pattern 2 \u2014 method call (implicit binding)</h4>\n<pre><code>const user = {\n  name: \"Devansh\",\n  greet() { console.log(`Hi, ${this.name}`); },\n};\nuser.greet();   // \"Hi, Devansh\"</code></pre>\n<p>There is a dot, so <code>this</code> is the object immediately to the <strong>left of the final dot</strong>:</p>\n<pre><code>app.user.greet();   // this is app.user, not app</code></pre>\n<p>The critical thing: <strong>this connection is created at the call site and exists only for that call.</strong> It is not stored anywhere. The function does not remember it.</p>\n\n<h4>Pattern 3 \u2014 new (construction binding)</h4>\n<pre><code>function Job(title) { this.title = title; }\nconst j = new Job(\"Backend Engineer\");</code></pre>\n<p><code>new</code> creates a brand new empty object and binds <code>this</code> to it for the duration of the call. Node 6 traces all four steps; here the relevant fact is that <code>new</code> overrides the other patterns.</p>\n\n<h4>Pattern 4 \u2014 explicit binding (call, apply, bind)</h4>\n<pre><code>function greet(greeting, punctuation) {\n  console.log(`${greeting}, ${this.name}${punctuation}`);\n}\nconst user = { name: \"Devansh\" };\n\ngreet.call(user, \"Hi\", \"!\");      // arguments listed individually\ngreet.apply(user, [\"Hi\", \"!\"]);   // arguments as an array\nconst bound = greet.bind(user);   // returns a NEW function, doesn't call\nbound(\"Hi\", \"!\");</code></pre>\n<p><code>call</code> and <code>apply</code> are identical except for argument form \u2014 a mnemonic that works: <strong>a</strong>pply takes an <strong>a</strong>rray.</p>\n<p><code>bind</code> is the important one: it does not call the function, it returns a <em>new</em> function permanently tied to that <code>this</code>. Binding is permanent \u2014 <code>bound.call(userB)</code> still uses the original binding.</p>\n<p><code>bind</code> also supports <strong>partial application</strong>: arguments passed after the <code>this</code> value are pre-filled. <code>const sayHi = greet.bind(user, \"Hi\")</code>.</p>\n<p><code>apply</code> had a second historical use before spread existed: <code>Math.max.apply(null, numbers)</code> was the only way to pass an array as individual arguments. Spread replaced that in 2015.</p>\n<p><strong>Precedence:</strong> <code>new</code> beats <code>bind</code>, which beats a dot, which beats a plain call.</p>\n\n<h4>The bug: losing this</h4>\n<pre><code>const user = { name: \"Devansh\", greet() { console.log(this.name); } };\n\nuser.greet();          // \"Devansh\"\nconst g = user.greet;  // copies the function value \u2014 no object attached\ng();                   // TypeError: Cannot read properties of undefined</code></pre>\n<p>Functions are values (Node 3). <code>const g = user.greet</code> copies <em>the function</em>, not any relationship to <code>user</code>. There was never a stored connection to copy.</p>\n<pre><code>setTimeout(user.greet, 100);                    // broken\nbutton.addEventListener(\"click\", this.handle);  // broken\njobs.map(formatter.format);                     // broken</code></pre>\n<p>Every one is the same thing: the function value is handed over, and later called with no dot. This is the single most common <code>this</code> bug in real code, and why you see <code>.bind(this)</code> scattered through older React class constructors.</p>\n<pre><code>class Scanner {\n  constructor(name) { this.name = name; }\n  scan() { console.log(`scanning ${this.name}`); }\n}\nconst fn = new Scanner(\"greenhouse\").scan;\nfn();   // TypeError \u2014 class bodies are strict, so this is undefined</code></pre>\n<p>Classes change nothing about how <code>this</code> works.</p>\n\n<h4>The three fixes, and when to use which</h4>\n<p><strong>Wrap in an arrow</strong> \u2014 keeps the dot intact at call time. The default choice:</p>\n<pre><code>setTimeout(() =&gt; user.greet(), 100);</code></pre>\n<p><strong>Bind</strong> \u2014 produces a permanently attached standalone function. Essential when you need to <em>remove</em> an event listener later, since <code>removeEventListener</code> requires the exact same reference:</p>\n<pre><code>class Dashboard {\n  constructor() {\n    this.handleClick = this.handleClick.bind(this);   // one stable bound function\n  }\n  mount(el) { el.addEventListener(\"click\", this.handleClick); }\n  unmount(el) { el.removeEventListener(\"click\", this.handleClick); }\n}</code></pre>\n<p>Writing <code>el.addEventListener(\"click\", this.handleClick.bind(this))</code> creates a <em>new</em> function each time, so removal silently fails and you have Stage 9's leak. A real trap.</p>\n<p><strong>Class field with an arrow</strong> \u2014 the modern shape:</p>\n<pre><code>class Dashboard {\n  handleClick = () =&gt; {\n    console.log(this.jobs.length);   // lexical, always the instance\n  };\n}</code></pre>\n\n<h4>Arrow functions: why they fix it</h4>\n<p>An arrow function has <strong>no <code>this</code> of its own</strong>. Not a different one \u2014 the binding simply does not exist. So <code>this</code> inside an arrow resolves exactly as any free variable would: by walking outward through the scope chain from Node 4.</p>\n<pre><code>scanAll() {\n  this.sources.forEach(function (s) {\n    console.log(this.name, s);   // undefined \u2014 separate function, plain call\n  });\n}\n\nscanAll() {\n  this.sources.forEach((s) =&gt; {\n    console.log(this.name, s);   // correct \u2014 arrow looks outward to scanAll's this\n  });\n}</code></pre>\n<p><strong>Where arrows are wrong</strong>, the mirror image of the same rule:</p>\n<pre><code>const user = {\n  name: \"Devansh\",\n  greet: () =&gt; { console.log(this.name); },   // NOT the user object\n};</code></pre>\n<p>The arrow was written at module top level, so its <code>this</code> comes from module scope \u2014 <code>undefined</code> in an ES Module. An object literal is not a scope and creates no <code>this</code>. So an arrow as an object's own method never works.</p>\n<p>Arrows also cannot be constructors, have no <code>arguments</code>, and cannot be generators. The reason for the first is now visible: <code>new</code> works by binding <code>this</code>, and an arrow has no <code>this</code> to bind.</p>\n\n<h4>Two special cases worth knowing</h4>\n<p><strong>In a DOM event handler written as a regular function</strong>, <code>this</code> is the element the listener was attached to \u2014 the browser calls it with explicit binding. With an arrow, <code>this</code> is lexical and you use <code>event.currentTarget</code> instead.</p>\n<p><strong>At a module's top level</strong>, <code>this</code> is <code>undefined</code> in an ES Module and <code>module.exports</code> in CommonJS. In a plain browser script it is the global object. A Node 13/17 boundary showing through.</p>\n\n<h3>In your project</h3>\n<p>Stage 2 of <code>shortlist</code> is designed around this node. You build <code>Source</code> adapters as classes, and at some point you will pass an adapter method as a bare reference and get <code>Cannot read properties of undefined (reading 'name')</code>.</p>\n<p>The instruction is to fix it three ways \u2014 arrow wrapper, <code>.bind()</code>, and restructuring so the method is never detached \u2014 understand why each works, and keep the one that reads best. Having produced the bug and all three fixes yourself is what turns this from a memorised list into something you can reason about under pressure.</p>\n<p>Stage 7 brings it back in browser costume: passing <code>this.handleFilterChange</code> to <code>addEventListener</code> loses <code>this</code> identically. And Stage 9's listener leak has a <code>this</code>-shaped cause: <code>addEventListener(\"click\", this.handle.bind(this))</code> creates a fresh bound function every call, so the matching <code>removeEventListener</code> never finds the same reference and the old listener stays registered forever. That is the connection between this node and the memory node.</p>\n\n<h3>In qbank</h3>\n<p>If any part of qbank instantiates a class and passes one of its methods as a callback \u2014 to Fastify as a route handler, to BullMQ as a processor, to an event emitter \u2014 that is exactly this situation. Worth grepping for <code>.bind(</code>: every occurrence marks a place where this problem was hit.</p>\n<p>If there are none and no classes, then qbank is written in a function-and-closure style rather than a method style, which is itself a fine thing to be able to say. Much modern backend JavaScript avoids <code>this</code> almost entirely by using plain functions and closures (Node 4) instead of classes. \"qbank uses closures over a Fastify instance rather than class methods, so <code>this</code> doesn't really appear\" is a stronger answer than pretending to a <code>this</code>-heavy design you do not have.</p>\n\n<h3>What an interviewer does with this</h3>\n<p><strong>\"How is <code>this</code> determined?\"</strong> Near-universal. Weak answers describe one case. A strong answer states the rule first \u2014 determined by how the function is called, not where it is written \u2014 then gives the four patterns in order: plain call (<code>undefined</code> in strict mode), method call (object left of the dot), <code>new</code> (the new object), explicit binding via <code>call</code>/<code>apply</code>/<code>bind</code>. Then add that arrows opt out entirely and resolve <code>this</code> lexically.</p>\n<p><strong>\"<code>call</code> vs <code>apply</code> vs <code>bind</code>?\"</strong> <code>call</code> and <code>apply</code> invoke immediately, differing only in argument form \u2014 apply takes an array. <code>bind</code> does not invoke; it returns a new permanently-bound function. Adding partial application, and that binding cannot be undone, beats a memorised one-liner.</p>\n<p><strong>\"Why do arrows behave differently with <code>this</code>?\"</strong> They have no <code>this</code> binding at all, so it resolves lexically through the scope chain. Give the consequence in both directions: correct for callbacks inside methods, wrong as an object's own method.</p>\n<p><strong>\"Here's some code \u2014 what does <code>this</code> refer to?\"</strong> Work through it out loud: was there a dot? a <code>new</code>? a bound function? is it an arrow? Naming the pattern you are applying is more convincing than producing the right answer silently.</p>\n<p><strong>\"You get <code>Cannot read properties of undefined</code> passing a method to <code>setTimeout</code>. Why?\"</strong> Passing <code>user.greet</code> passes the function value alone; there was never a stored link, so at call time there is no dot and <code>this</code> is <code>undefined</code>. Fixes: arrow wrapper, <code>.bind(user)</code>, or a class field arrow. Mentioning that inline <code>.bind()</code> in <code>addEventListener</code> breaks later removal marks real experience.</p>\n"
    }
  ]
}